# Streamhaul — Implementation Plan & Tracker

**This is the living tracking document for building Streamhaul.** It decomposes the design in
[`LLD.md`](./LLD.md) into PR-sized tasks across the 8 build phases. Update it in the **same PR** that
completes a task (one task ≈ one PR). It is the source of truth for "what's done, what's next."

- **Design:** [`LLD.md`](./LLD.md) · **Product:** [`PRD.md`](./PRD.md) · **Rules:** [`CLAUDE.md`](./CLAUDE.md)
- **Every task** follows the CLAUDE.md quality gate: implement → tests → **`bug-bot`** → **`code-reviewer`**
  (→ `security-engineer` if it touches crypto/auth/transport) → fix → PR → green CI → merge. No exceptions.

## Status legend
`☐` Todo · `🟡` In progress · `✅` Done · `⛔` Blocked · `🔬` In review/gate

## How to use
1. Pick the next unblocked task whose dependencies are `✅`.
2. Branch `‹type›/‹task-id›-slug` (e.g. `feat/P0-3-shp-video-header`).
3. Implement to the task's **Exit criteria** + **Tests**; run the quality gate.
4. In the PR, flip the task to `✅` and fill the **PR** column. Update the phase gate when all its tasks pass.

---

## Milestone Overview

| Phase | Theme | Gate (exit) | Status |
|-------|-------|-------------|:------:|
| **P0** | Hello Pixels (latency lab) | Live Win→native render, ≤~30ms glass-to-glass LAN @60fps, zero-copy DXGI→NVENC, 10-min stable | 🟡 |
| **P1** | Input + multi-channel + audio | Click-to-photon measured; audio AV-synced; input never starved by video | 🟡 |
| **P2** | Adaptivity (Game/Work) | Smooth adapt under loss/bandwidth caps; mode switch no flapping; loss recovery works | 🟡 |
| **P3** | Security & pairing | First-pair TOFU pins key; unpinned MITM rejected; all channels E2E; rotation tested | 🟡 |
| **P4** | Connectivity (WebRTC+relay) | Connects across symmetric NAT via relay; relay carries only opaque ciphertext | ✅ |
| **P5** | Browser client | Chrome/FF/Safari view+control via H.264; same signaling/relay path | 🟡 |
| **P6** | Cross-OS hosts | macOS + Linux hosts zero-copy capture→encode; host↔client matrix green | ☐ |
| **P7** | File transfer | Large transfer doesn't degrade video QoE; resumable; integrity-verified | ☐ |
| **P8** | QUIC promotion + mobile | Native↔native auto-selects QUIC, survives network change; mobile thin clients | ☐ |

**Progress:** Phase 0 complete (P0-1…P0-10; P0-6/7/8/10 via portable software paths, real DXGI/NVENC/wgpu + LAN-budget deferred to the on-hardware session). **Phase 1: P1-1, P1-2, P1-4, P1-5 done; P1-3 partial (🟡); Gate P1 click-to-photon proxy measured.** **Phase 3 COMPLETE (#28–#32): P3-1 device identity + Keystore (🟡 SW; HW deferred R-HW-KS), P3-2 Noise handshake + BindCert, P3-3 SAS + SPAKE2 pairing, P3-4 per-channel ChaCha20-Poly1305 + key rotation, P3-5 authorization + kill-switch — each through the full bug-bot + code-reviewer + security-engineer triple-gate (ADRs 0006–0010). Phase 2: P2-1 (SCReAM), P2-2 (rate allocator), P2-3 (content classifier), P2-4 (double-buffered mode switch), P2-5 (codec negotiation + HEVC flag), and P2-6 (loss recovery) — **all 6 merged** (#20–#25), each through the full bug-bot + code-reviewer (+ security-engineer/rust-staff where applicable) gate. Adaptivity is verified in simulation / portable form; real-network adaptation, NVENC pixel-format reconfigure (R6), and AV1/HEVC HW encoders (R-CODEC) land in the on-hardware session.** **Phase 4 COMPLETE (#34–#39): P4-1 zero-knowledge `sh-signaling` (self-hostable WS server + client, opaque SDP/ICE envelope routing, trickle-ICE, reconnect with injectable backoff, spoof rejection, fuzz target; ADR-0011; deferred live-WSS/TLS R-SIG-TLS) — **now hardened with R-SIG-AUTH (ADR-0016): cryptographic peer authentication via a server-challenged Ed25519 possession-of-identity-key proof (`IdentityProofAuthenticator`), closing the `AcceptAll`-only hole; `peer_auth_decode` fuzz target; gated**, P4-2 `sh-ice` (STUN RFC 8489 codec + ICE RFC 8445 agent + NAT simulator + relay steering, constant-time MESSAGE-INTEGRITY, fuzz target; ADR-0012), P4-3 TURN client RFC 8656 (`TurnClient` Unallocated→Allocating→Allocated via 401-challenge, `SimTurnServer`, `add_relay_candidate()` so symmetric×symmetric NAT connects exclusively via relay — `symmetric_symmetric_via_relay_connected` + `nat_matrix_relay_fallback`), P4-4 WebRTC backend (`str0m` sans-IO `WebRtcTransport`+`WebRtcChannel` behind the `Transport`/`Channel` traits) + GCC congestion control (ADR-0013), P4-5 DTLS↔device-identity binding (whole-cert SHA-256 commit in the Noise-delivered identity-signed `BindCert`; a signaling/SDP fingerprint swap is fail-closed by str0m; `ALG=NONE` downgrade is a typed abort — kills the WebRTC signaling MITM; ADRs 0007-amended/0014), and P4-6 transport-capability negotiation (`transports:[quic,webrtc]`, symmetric fixed-preference `negotiate()`) + the `sh-core` `SessionEstablisher` session-orchestration seam that enforces the DTLS pin-gate non-bypassably at the negotiation boundary (ADR-0015) — each through the full bug-bot + code-reviewer + security-engineer triple-gate. The gate repeatedly caught real defects that passed the initial tests: a non-converging GCC behind a luck-based convergence test, untested transport mappings behind a vacuous loopback, a security-API footgun that could silently skip the DTLS binding, and a runtime-starvation flake. Deferred to P5/GA (do NOT reopen the security guarantees): live UDP + tokio drive loop (R-WEBRTC-LIVE), live coturn (R-COTURN-DEPLOY), browser↔native interop + ICE-stack unification (P5), the Noise↔DTLS-exporter binding (R-DTLS-EXPORTER-BIND). **`PinnedWebRtcTransport` builder DONE (pre-P5 follow-up on `feat/pinned-webrtc-transport`)**: `WebRtcTransport` is now `pub(crate)`; the only public path to a `Transport`-implementing WebRTC type is `WebRtcTransportBuilder::pin_remote_dtls(fp)` — the type system enforces the security invariant that was previously contractual-only. ADR-0017.** **Phase 5 in progress: P5-1 🟡 (wire-parity codec bridge `sh-wasm`, 41 tests, ADR-0019). P5-1b 🟡 (browser crypto bridge `sh-crypto-wasm`: device identity, R-SIG-AUTH, full wasm-path Noise XK/IK + DTLS binding, 23 tests, private keys in-wasm, WebCrypto CSPRNG (entropy-probed, no traps), cross-target parity proven; ADR-0020; gated). The full `sh-crypto` stack (incl. `snow`/`spake2`) compiles to wasm32, so the browser runs the *complete* identity-bound handshake — not a downgraded path. P5-3 Stage 2 ✅: the **identity-bound browser↔native live session** — a real Firefox `WebClient` ↔ native `streamhaul-webrtc-host` over the live zero-knowledge `sh-signaling` relay, with the DTLS pin sourced from the **Noise BindCert** (live XK over a new opaque `MessageKind::Noise`), and a **non-vacuous signaling/SDP MITM rejection** (headless Firefox, 5× no-flake). This is the security capstone of Phase 5: the browser↔native session is now MITM-protected by the same DTLS↔device-identity binding as the native transport (P4-5). Deferred: live-signaling R-SIG-AUTH on this path (R-SIG-AUTH-LIVE — separable), persistent TOFU, Chrome/Safari (R-BROWSER-MATRIX), coturn (R-COTURN-DEPLOY). ADR-0023.** — `run_input_rtt_harness` (`feat/P1-gate-input-rtt`) delivers 200-event loopback RTT: p50 = 722 µs, p95 = 1,117 µs over the reliable Input channel (true per-event serialized RTT; previous batch-send numbers of p50 = 4.6 ms, p95 = 5.4 ms reflected queue-drain time, not true transport RTT); P1-3 ships the portable `InputInjector` trait + `CoordMapper` + mocks in `sh-input`, but real platform injection (the click-to-photon enabler) is deferred — R14; P1-4 portable audio + `AvSync` done, real WASAPI/Opus deferred — R13. The full portable Phase-0 vertical slice runs end-to-end and is measured (loopback); Phase-1 input/control framing, multi-channel transport, input-injection seam, prioritization, and audio AV-sync are landed and gated.

> **Phase-0 local-vs-hardware note (overnight build):** the dev laptop is **Linux/Intel iGPU, no Windows SDK, no NVIDIA, no cmake/nasm/clang**, so the *real* hardware paths — DXGI capture (P0-6), NVENC encode (P0-7), wgpu-on-display (P0-8) — cannot be built or verified here. The overnight work delivers a **portable, pure-Rust software pipeline** (synthetic capture → raw codec → loopback QUIC → decode → headless sink → latency harness) that runs and is measured **locally and in CI**, achieving Phase 0's *purpose* (validate the vertical-slice latency budget). The hardware backends slot in behind the same traits during the on-hardware/LAN session.

---

## Cross-Cutting Workstreams (run continuously, not a phase)

| ID | Workstream | Notes | Agent | Status |
|----|-----------|-------|-------|:------:|
| X-1 | **CI activation** | ✅ Live: `pr-title`/`lint`/`test` (3 OSes)/`audit` now run real Rust gates; toolchain pinned (1.95.0). **Coverage gate now DONE (per-crate):** the `coverage` CI job runs `cargo llvm-cov --fail-under-lines` on each core crate independently (NOT an aggregate, so one crate can't regress behind another) — `sh-protocol` ≥92% (current ≈99.9%) and `sh-crypto` ≥90% (current ≈96%), own-code only (`sh-types` path-dep excluded), `--all-features`. Meets/exceeds CLAUDE.md §5's ≥85% line floor; a PR can no longer silently lower coverage on these crates. **Pending:** (a) raise `sh-transport` (currently ≈74% — live-network files connection/endpoint/quic_binding are R-WEBRTC-LIVE-deferred 0%, and channel/driver are under-tested) to the §5 bar, then add it to the gate; (b) full-workspace cross-OS clippy on the core crates (today the `platform-*` jobs run `clippy -D warnings` per-OS only on their own shim crate; the whole-workspace `lint` clippy runs Linux-only); (c) an MSRV-verification job. | devops-engineer | 🟡 |
| X-2 | **Testing infra** | `LoopbackTransport`, injected `Clock`+seedable RNG, `proptest`, `cargo-fuzz` targets, `loom` for lock-free queues. Build incrementally with each crate. **Started:** proptest across sh-types/sh-protocol/sh-codec-hw/sh-core; `cargo-fuzz` targets `shp_decode` (P0-3) and `fuzz_reassembler_ingest` (P0-9). **CI fuzz-target compile-check now DONE:** the `fuzz-build` CI job runs `cargo +nightly fuzz check` over all 8 excluded fuzz crates (19 targets across sh-protocol/sh-core/sh-transport/sh-codec-hw/sh-crypto/sh-signaling/sh-ice/sh-web-client) on every PR/push, so a fuzz target that stops compiling can no longer rot undetected (previously caught only by hand in P3-3). The job auto-discovers `crates/*/fuzz` (no hardcoded list to drift) and pins `cargo-fuzz`. **Scheduled nightly fuzz *run* now DONE:** a separate `fuzz-nightly` workflow (`schedule` 06:00 UTC + `workflow_dispatch`) actually *runs* libFuzzer against every target for a bounded time, aggregates failures across all targets (doesn't stop at the first crash), and uploads any reproducing crash input as an artifact — the continuous fuzzing §5 mandates for hostile-input parsers. (Dispatch-verified: the run surfaced that `sh-web-client`'s `parse_sdp_fingerprint` target couldn't *execute* on the host — its `#[wasm_bindgen]` entry aborts in wasm-bindgen glue off-wasm — so a host-callable `parse_sdp_fingerprint_host` seam over the same pure parser was added and the target repointed; the SDP fingerprint parser is now genuinely fuzzed, not just compile-checked.) **Coverage gate now DONE** (see X-1: per-crate `cargo llvm-cov --fail-under-lines` on sh-protocol/sh-crypto). **Fuzz-lockfile `cargo audit` now DONE:** the `audit` CI job audits every committed `crates/*/fuzz/Cargo.lock` (auto-discovered, `--deny warnings`) in addition to the root lockfile — the excluded fuzz crates pull their own deps (libfuzzer-sys, arbitrary + the parent crate's tree) the workspace audit doesn't see; all 8 are clean. **Fuzz corpus persistence now DONE:** the `fuzz-nightly` workflow restores the prior corpus via a rolling cache (unique run-id `key`; `restore-keys: fuzz-corpus-` seeds from the most recent prior run) and **saves it unconditionally** (split `actions/cache/restore` + `actions/cache/save` with `if: always()`, NOT the combined action whose post-save is skipped on a failed job — that would discard the coverage-rich corpus on exactly the nights a target crashed), so coverage ACCUMULATES across every night, including red ones, instead of cold-starting. Corpus size is logged each run for early-warning. **Pending:** corpus minimization (`cargo fuzz cmin`) + a committed seed corpus to bound the rolling cache against the 10 GB Actions budget and give cache-miss runs a productive floor; `loom` is **not applicable as scoped** — there are no lock-free queues, and the one atomic primitive (`authz::epoch` kill-switch floor) lives in `sh-core`, whose `tokio` dependency disables `tokio::net` under a global `--cfg loom`, so loom-testing it in place is infeasible (extracting a crate solely to model-check one `fetch_max` is disproportionate; the `SeqCst` `fetch_max` monotonicity is correct-by-construction + documented). | qa-engineer, rust-staff-engineer | 🟡 |
| X-3 | **Security review cadence** | `security-engineer` reviews **every** crypto/auth/transport PR; `cargo audit` clean; document `snow` unaudited status. | security-engineer | ☐ |
| X-4 | **Release engineering** | `xtask` for packaging/signing; signed releases (Sigstore/cosign), SBOM (CycloneDX), per-OS installers/services. | devops-engineer | ☐ |
| X-5 | **Open decisions** | Resolve LLD §9 items before they block: Noise pattern names (SHA-256, before P3), platform-attestation envelope, UGC lifetime per compliance tier, `str0m` Safari interop (before P4/5). | software-architect, security-engineer | ☐ |

---

## Phase 0 — Hello Pixels (latency lab)

> Goal: prove the latency budget on the thinnest vertical slice. Windows host → native client, LAN, H.264,
> **bare `quinn` QUIC (no ICE/crypto)**, no adaptivity. Validates codec/render pipeline in isolation.
>
> **Scaffolding note (P0-1):** the workspace ships 10 portable `sh-*` libs + 2 bins. The platform/codec
> crates `sh-codec-hw` (P0-7) and `sh-platform-win` (P0-6), and the bindings `sh-ffi`/`sh-wasm` (P5/P8),
> are created **with their tasks** — they need real platform code to compile cross-OS, so adding empty
> stubs now would break `cargo test` on the Linux/macOS CI runners.

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P0-1 | Cargo workspace scaffold + workspace lints (panic-ban) + activate CI | (all skeleton) | — | rust-staff-engineer | crates compile; clippy `-D warnings` clean; CI goes live | ✅ | #5 |
| P0-2 | `sh-types`: IDs, units, `FrameId`/`Timestamp`/`ChannelId`, error scaffolding | sh-types | P0-1 | rust-staff-engineer | unit | ✅ | #5 |
| P0-3 | `sh-protocol`: common header + video payload header (per LLD §3.1), encode/decode | sh-protocol | P0-2 | rust-staff-engineer, network-engineer | **proptest** (never-panic + roundtrip) + **cargo-fuzz** target | ✅ | #6 |
| P0-4 | `sh-transport`: bare `quinn` backend (LAN, datagrams), no ICE/crypto | sh-transport | P0-2 | network-engineer | loopback integration | ✅ | #7 |
| P0-5 | `sh-media`: `ScreenCapturer`/`VideoEncoder`/`VideoDecoder` traits + frame/surface types | sh-media | P0-2 | realtime-systems-engineer | trait doubles | ✅ | #8 |
| P0-6 | Capture. **Portable `SyntheticCapturer` done + tested (local/CI), #8.** Real **DXGI Desktop Duplication** (`sh-platform-win`, zero-copy D3D11) is **deferred to the on-hardware session** — the dev laptop is Linux/Intel with no Windows SDK, so it can't be built/verified here. | sh-media / sh-platform-win | P0-5 | realtime-systems-engineer | manual + smoke (on hardware) | 🟡 | #8 |
| P0-7 | Codec. **Portable lossless `RawEncoder`/`RawDecoder` (+ `Codec::Raw`) done + tested (local/CI), #9.** Real **NVENC H.264** encode + HW decode (zero-copy surface registration) is **deferred to the on-hardware session** (no NVIDIA/Windows SDK/C build tooling on the dev laptop). | sh-codec-hw | P0-5 | realtime-systems-engineer | encode/decode roundtrip | 🟡 | #9 |
| P0-8 | Sink. **Headless `FrameSink` + `CollectingSink`/`NullSink` done + tested (in `sh-media`), #10.** Real **`wgpu` NV12→RGB present + latency overlay** (display) is **deferred to the on-hardware session**. | sh-media / sh-render | P0-5 | ui-engineer, realtime-systems-engineer | manual (on hardware) | 🟡 | #10 |
| P0-9 | **End-to-end wiring done + tested, #10.** `sh-core` packetize (SHP fragmentation + reorder-tolerant `Reassembler`) + async host/client pipelines; `streamhaul-host`/`streamhaul-client` bins runnable for a real LAN run. Real DXGI/NVENC/wgpu backends plug in behind the same traits. | bins, sh-core | P0-3,4,7,8 | rust-staff-engineer | e2e smoke | ✅ | #10 |
| P0-10 | **Loopback latency harness done + measured locally, #10/#11** (`run_loopback_harness`: 120 single-datagram frames, lossless among received, latency reported; deterministic + fast). The client tolerates datagram loss (returns partial) — multi-fragment reassembly is covered by packetize unit/proptests. **Real LAN + hardware glass-to-glass budget + 10-min soak are the user's LAN session.** | host/client | P0-9 | performance-tuning-engineer | latency bench; soak (LAN) | 🟡 | #10, #11 |

**Gate P0:** ☑ data-path slice runs + lossless (loopback) · ☐ ≤~30ms glass-to-glass **LAN** (user's session) · ☐ zero-copy DXGI→NVENC (hardware) · ☐ 10-min soak (LAN).

> **LAN test handoff (run when awake):** on the host machine `cargo run -p streamhaul-host --features sh-transport/insecure-lan -- 0.0.0.0:7878`; on the client `cargo run -p streamhaul-client --features sh-transport/insecure-lan -- <host-ip>:7878`. Both already apply `lan_lab_transport_config()` (datagrams enabled) via the insecure config helpers. The client prints received-frame/latency stats. (This is the LAN-lab insecure path — `compile_error!` blocks it from release builds.)

---

## Phase 1 — Input + multi-channel + audio

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P1-1 | Promote `Transport`/`Channel` trait + `ChannelSpec`; multi-channel (video unreliable + input reliable, **input urgency 0**) | sh-transport | P0 | network-engineer, rust-staff-engineer | loopback multi-channel | ✅ | #15 |
| P1-2 | `sh-protocol`: input event message (LLD §3.1) + control/RPC framing | sh-protocol | P0-3 | network-engineer | proptest + fuzz | ✅ | #14 |
| P1-3 | `sh-input`: portable `InputInjector` trait + `CoordMapper` (normalized→pixel, multi-monitor, i32 origins) + `NoopInjector` + `RecordingInjector` mocks. Real platform injection (Windows `SendInput`/Raw Input, Linux `uinput`, macOS `CGEvent`) deferred to `sh-platform-*` — see R14. | sh-input (trait/mocks); sh-platform-win/linux/mac (impls, deferred) | P1-1,P1-2 | realtime-systems-engineer | 27 unit + 1 doc-test; proptest mapped-coords-in-bounds | 🟡 | #18 |
| P1-4 | Audio: capture + encode/decode + AV sync (shared monotonic clock). **Portable slice done**: `AudioFrame`/`AudioEncoder`/`AudioDecoder` traits + `AudioCodec` + raw-PCM codec + `SyntheticAudioSource` + `AvSync` controller (±20ms, max skew 18.4ms). **Deferred** (no audio hardware on dev box): real WASAPI loopback capture + Opus — see note. | sh-media, sh-codec-hw | P0 | realtime-systems-engineer | sync test + raw-audio fuzz | ✅ | #17 |
| P1-5 | Channel prioritization (input > video) + file-channel congestion-isolation scaffolding | sh-transport | P1-1 | network-engineer | starvation test under load | ✅ | #16 |

**Gate P1:** 🟡 click-to-photon: **input round-trip latency measured over the reliable Input channel (loopback proxy: p50 = 722 µs, p95 = 1,117 µs, min = 483 µs, max = 2,234 µs, 200/200 events delivered in order)**. Measurement uses true per-event serialized RTT (send event i, await echo, then send event i+1); previous numbers (p50 = 4,627 µs, p95 = 5,357 µs) reflected batch-send queue-drain time, not the real transport contribution. True glass-to-photon deferred to the on-hardware session (needs real injection R14 + capture/encode P0-6/7/8). Cross-reference R9 (one-way latency needs clock sync; RTT is symmetric here). · ☑ audio AV-synced (±20ms) (`AvSync` controller; max skew 18.4ms < 20ms target, deterministic over 3 runs; test in `sh-media`) · ☑ input not starved under video load (structural per-stream isolation; loopback test #16).

> **Datagram demux** (route datagrams to the right channel by SHP CHANNEL field — needed once video+audio coexist) and a **bandwidth-shaped congestion-scheduling test** (loopback can't create real congestion) remain follow-ups (see Risk Register).
>
> **P1-4 audio hardware deferred** (R13): the portable software path (synthetic source → raw-PCM codec → `AvSync`) lands now so the pipeline is measurable on any machine incl. CI; real **WASAPI loopback capture** + **Opus** encode/decode arrive with platform crates (no audio capture hardware / Opus toolchain on the dev box). The trait seams (`AudioEncoder`/`AudioDecoder` + `AudioCodec`) are designed so Opus drops in without touching callers.
>
> **P1-3 platform injection deferred** (R14): `sh-input` delivers the portable `InputInjector` trait, `CoordMapper`, `NoopInjector`, and `RecordingInjector`. Real injection (`SendInput`/Raw Input on Windows, `uinput` on Linux, `CGEvent` on macOS) is deferred to `sh-platform-*` crates — no injection hardware available on the dev box. The trait seam is designed so platform crates drop in without touching callers.
>
> **Gate P1 click-to-photon proxy** (`run_input_rtt_harness`, `feat/P1-gate-input-rtt`): `sh-core` gains `input_harness.rs` (gated under `harness` feature). Client opens the reliable Input channel, sends 200 distinct `InputEvent`s (index encoded in `pointer_x`), host injects via `RecordingInjector` + echoes each raw 16-byte event back, client computes per-event RTT. RTT is measured with a serialized per-event round-trip (send event i, block waiting for echo, then send event i+1), yielding the true transport contribution rather than batch-send queue-drain time. Measured on dev box (Linux loopback): **p50 = 722 µs, p95 = 1,117 µs, min = 483 µs, max = 2,234 µs; 200/200 delivered in order; zero loss** (reliable channel guarantee). Network + transport contribution to click-to-photon is bounded at ~1.1 ms p95 on loopback; on a real LAN this will be dominated by the actual RTT (~0.5–2 ms) with protocol overhead well under 1 ms additional. (Previous numbers p50 = 4,627 µs, p95 = 5,357 µs reflected batch-send queue-drain inflation and did not represent true per-event transport RTT.)

---

## Phase 2 — Adaptivity (Game/Work modes)

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P2-1 | `sh-adaptive`: `CongestionController` trait + **SCReAM** (native path) | sh-adaptive | P1 | network-engineer | sim: adapt to bandwidth caps | ✅ | #20 |
| P2-2 | Rate allocator across channels (video/audio/file budgets) | sh-adaptive | P2-1 | network-engineer | allocation unit | ✅ | #21 |
| P2-3 | Content classifier (4-signal heuristic + hysteresis FSM, LLD §5.2) | sh-adaptive | P1 | realtime-systems-engineer | FSM unit (no flapping) | ✅ | #22 |
| P2-4 | Encoder reconfigure + **double-buffered mode switch** (4:2:0↔4:4:4, glitch-free) | sh-codec-hw | P2-3 | realtime-systems-engineer | switch test; NVENC session-limit guard | 🟡 | #24 |
| P2-5 | HEVC enable (commercial build feature flag) + codec negotiation/degradation ladder (ADR-0004) | sh-codec-hw, sh-protocol | P0-7 | realtime-systems-engineer | negotiation matrix | 🟡 | #25 |
| P2-6 | Loss recovery: rolling intra-refresh + adaptive FEC + selective NACK + forced IDR (LLD §4.4) | sh-adaptive, sh-protocol | P2-1 | network-engineer, realtime-systems-engineer | induced-loss recovery test | ✅ | #23 |

**Gate P2:** 🟡 smooth adapt under loss/caps (SCReAM sim verified, P2-1 ✅) · 🟡 cross-channel rate allocator (priority-ordered floors + video bulk + file leftover, P2-2 ✅) · ✅ Game↔Work no flapping (hysteresis FSM + 93 deterministic tests, P2-3 ✅) · ✅ keyframe/loss recovery verified (P2-6: tiered RTT-band policy, IDR suppression, rolling intra-refresh, NACK bitmap, gap detector — 20 tests all pass; escalation trace NACK→FEC→IDR confirmed) · 🟡 glitch-free double-buffered mode switch (P2-4: portable orchestration — `SessionLimiter`, `EncoderFactory`, `DoubleBufferedEncoder`, `BackpressurePolicy` — fully tested against `RawEncoder`; 37 unit + 7 doc-tests green; real NVENC 4:2:0↔4:4:4 hardware reconfigure deferred — see R6) · 🟡 codec negotiation ladder + HEVC feature flag (P2-5: `CodecNegotiator`/`CodecCapabilities`/`BuildFlavor` in `sh-codec-hw`; `CodecCapsPayload` binary wire format + `decode_caps`/`encode_caps` in `sh-protocol::capability`; `hevc` Cargo feature OFF=OSS / ON=Commercial; full negotiation matrix + wire roundtrip tests all pass; real AV1/HEVC HW encoder backends deferred — see R-CODEC). **Progress: 6/6 tasks merged (#20–#25). Phase 2 complete (portable/sim slice); real-network + HW-encoder validation deferred to the on-hardware session — R6, R-CODEC.**

> **P2-6 FEC symbol codec deferred (R-FEC):** P2-6 delivers the adaptive `FecPolicy` ratio engine and `NackFeedback` wire framing. The actual Reed-Solomon / XOR FEC symbol encode/decode codec is deferred: it requires its own fuzz-heavy parser of untrusted repair-symbol bytes and is independent of the policy. See Risk Register entry R-FEC below.

---

## Phase 3 — Security & pairing (E2E)

> **Security applies from here on** (LLD §6). Every task in P3+ touching crypto requires `security-engineer` review.

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P3-1 | `sh-crypto`: Ed25519 device identity + `Keystore` trait + platform keystores (TPM/Keychain/DPAPI) | sh-crypto | P0 | security-engineer, rust-staff-engineer | unit + keystore mocks | 🟡 | #28 |
| P3-2 | Noise tunnel (`snow`, `Noise_XK` pair / `Noise_IK` connect) + identity-bound `BindCert` | sh-crypto, sh-transport | P3-1 | security-engineer | handshake unit + **fuzz** | ✅ | #29 |
| P3-3 | TOFU pinning + SAS (from Noise hash) + PAKE pairing codes (SPAKE2/OPAQUE) | sh-crypto | P3-2 | security-engineer | MITM-rejection test | ✅ | #30 |
| P3-4 | Channel encryption + key hierarchy + rotation (PFS ephemerals, rekey, channel subkeys) | sh-crypto, sh-transport | P3-2 | security-engineer | rotation test; negative tests | ✅ | #31 |
| P3-5 | Authorization (capability mask, host-enforced, non-escalatable) + kill-switch (RAM key zeroize) | sh-core | P3-4 | security-engineer | cap-guard + kill-switch test | ✅ | #32 |

**Gate P3:** 🟡 TOFU pins on first pair (P3-1: software-backed, hardware deferred) · ✅ unpinned-key MITM rejected (P3-2) · ✅ SAS + SPAKE2 PAKE pairing + revoke gate (P3-3) · ✅ all channels E2E encrypted (P3-4) · ✅ kill-switch verified. **Progress: 5/5 tasks (P3-1 🟡 — identity + trait + SW keystore delivered; TPM/Keychain/DPAPI/StrongBox deferred as R-HW-KS. P3-2 ✅ — Noise handshake + BindCert + 6-check verification + fuzz. P3-3 ✅ — SAS from `h` via HKDF-SHA-256 + SPAKE2 PAKE with explicit key-confirmation MAC bound to `h`+ids + TOFU pin gated on confirm + revoke-gate surfaced + `was_peer_revoked` Keystore method; spake2 unaudited — pre-GA R-SPAKE2-AUDIT. P3-4 ✅ — ChaCha20-Poly1305 AEAD per-channel, HKDF key hierarchy 12 subkeys from Noise PRK, ratchet chain 1-way HKDF, sliding anti-replay bitmap 1024-bit, epoch rekey + grace prior slot, two-phase commit-on-AEAD-success for both epoch advance and ratchet advance, `zeroize_all` kill-switch including PRK, clock-regression-safe `needs_rekey`, `channel_frame_open` fuzz target; 160 tests all green. P3-5 ✅ — Capabilities bitflags (VIEW/CONTROL/CLIPBOARD/FILE/AUDIO/ELEVATION), UGC canonical 73-byte TBS (encode+decode+verify, 5-check, constant-time grantee binding, unknown-bit truncation), SessionAuthorizer sealed AND-intersection + no-widen invariant + ELEVATION default-deny without FreshPresence + kill-switch (zeroize_all + bump_min_epoch + Denied::Killed), MinEpochStore trait + InMemoryMinEpochStore (R-EPOCH-PERSIST deferred), MalformedUgc/UgcBadSignature/UgcWrongGrantee/UgcExpired/UgcRevoked in CryptoError, ugc_decode fuzz target; 50 unit tests + 11 doc-tests all green. Deferred: R-EPOCH-PERSIST (durable min_epoch store), R-ELEVATION-MFA (WebAuthn/FIDO2 issuance). ADR-0010 status: Accepted.).**

---

## Phase 4 — Connectivity (WebRTC + signaling + relay)

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P4-1 | `sh-signaling`: client + self-hostable signaling server (WSS, SDP/ICE exchange, trickle) | sh-signaling | P3 | network-engineer | signaling integration | ✅ | #34 |
| P4-2 | `sh-ice`: ICE/STUN candidate gathering, connectivity checks, P2P-vs-relay nomination | sh-ice | P4-1 | network-engineer | NAT-sim matrix | ✅ | #35 |
| P4-3 | TURN client (RFC 8656): Allocate/Refresh/CreatePermission/ChannelBind codec + long-term credential auth + SimTurnServer + `latency-probe relay steering` (LLD §4.3); `IceAgent::add_relay_candidate()` wired + symmetric×symmetric end-to-end relay test + `nat_matrix_relay_fallback` matrix; coturn deploy **deferred** → R-COTURN-DEPLOY | sh-ice | P4-2 | network-engineer | 401-challenge + channel roundtrip + relay-steering + ICE-relay integration tests (63 ✅) | 🟡 | #36 |
| P4-4 | `sh-transport`: WebRTC backend (`str0m`) + **GCC** congestion control | sh-transport, sh-adaptive | P4-1 | network-engineer | webrtc transport+loopback (9 ✅, 1 slow `#[ignore]`) + GCC sim (15 ✅) | ✅ | feat/P4-4-webrtc-gcc |
| P4-5 | Bind DTLS fingerprint to device identity via signed `BindCert` (kills signaling MITM, LLD §6.2) — whole-cert SHA-256 commit (ADR-0014), pin-before-handshake, `ALG=NONE` downgrade rejected for WebRTC; `remote_dtls_fingerprint()` getter added; DTLS-exporter prologue binding deferred → R-DTLS-EXPORTER-BIND | sh-crypto, sh-transport | P3-2,P4-4 | security-engineer | fingerprint-swap rejection (integration: honest connects + str0m-verified pin == committed; MITM swap rejected; non-vacuity control) + BindCert/downgrade units | ✅ | |
| P4-6 | Transport capability negotiation (`transports:[quic,webrtc]`) + relay fallback path wiring + `sh-core` session-orchestration seam | sh-protocol, sh-types, sh-core | P4-4, P4-5 | network-engineer, security-engineer | `TransportCaps` codec round-trip/hostile-input/reserved-bits; `negotiate` QUIC-preferred/WebRTC-fallback/NoCommonTransport/symmetry proptest; `session_webrtc_dtls_pin_gate` (ALG=NONE → DtlsBindingMissing); `session_quic_no_pin_needed`; `session_negotiate_no_common_transport`; `session_symmetric_exchange` (both caps→QUIC); `session_mitm_dtls_cert_swap_rejected` (honest connects, MITM str0m fail-closes — non-vacuity); `session_webrtc_pin_propagated_to_factory` (verified pin reaches the factory through the seam — non-vacuity); `session_quic_with_unexpected_dtls_commitment_rejected` (QUIC peer with a DTLS commitment → hard abort); `relay_fallback_direct_path_selection` (is_relay flag surfaced); fuzz target `decode_transport_caps` (16 sh-core/sh-protocol tests, all ✅; suite deterministic over 20+ repeated runs) | ✅ | |

**Gate P4:** ✅ P2P when possible (NAT-sim matrix: FullCone×*, RestrictedCone×RestrictedCone, PortRestricted×PortRestricted → P2P nominated; Symmetric×Symmetric → relay fallback nominated — all verified in `agent::tests::nat_matrix`, P4-2 ✅) · ✅ connects across symmetric NAT via relay (`IceAgent::add_relay_candidate()` wired; end-to-end relay connectivity proven in `agent::tests::symmetric_symmetric_via_relay_connected` + `nat_matrix_relay_fallback` with SimTurnServer + NatSimNetwork Symmetric NAT, P4-3 ✅; relay fallback now **surfaced to the session orchestrator** via `IcePathOutcome.is_relay` in P4-6 — live coturn server wiring deferred → R-COTURN-DEPLOY) · ✅ WebRTC transport backend (`str0m` sans-IO, `WebRtcTransport`+`WebRtcChannel` implementing `Transport`+`Channel`, DataChannel loopback round-trip verified, `GccController` for WebRTC path, ADR-0013; P4-4) · ✅ DTLS-MITM closed — peer DTLS cert bound to device identity, fail-closed (P4-5); **DTLS pin gate now enforced in production code** (`sh-core::session` — not just the P4-5 integration test), non-bypassable at the negotiation seam (P4-6, ADR-0015): the WebRTC security gate `require_webrtc_dtls_pin()?` is the only path that builds a WebRTC transport; `ALG=NONE` downgrade is a typed `DtlsBindingMissing` abort. · ✅ Transport capability negotiation: `TransportCaps` 2-byte wire format (VERSION + bitmask, forward-compat reserved-bit policy), `negotiate(a,b)==negotiate(b,a)` symmetry proven by proptest, QUIC preferred over WebRTC for native↔native, WebRTC fallback for browser. ADR-0015. · 🟡 Deferred to P5/live: live DTLS/SRTP over real UDP socket (R-WEBRTC-LIVE), live coturn (R-COTURN-DEPLOY), browser↔native SDP interop, tokio drive task for WebRtcTransport production use, Noise-prologue↔DTLS-exporter channel binding (R-DTLS-EXPORTER-BIND — anti-downgrade gate now satisfied in code; the additional anti-lift binding remains deferred). **Phase 4 COMPLETE** (6/6 tasks: P4-1 ✅ P4-2 ✅ P4-3 🟡[live coturn deferred] P4-4 ✅ P4-5 ✅ P4-6 ✅): signaling + ICE/STUN/TURN (sim-verified) + WebRTC backend + DTLS identity binding + transport capability negotiation + session orchestration seam — all landed. Live network paths (R-WEBRTC-LIVE, R-COTURN-DEPLOY) and browser interop are the P5 scope.

---

## Phase 5 — Browser client

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P5-1 | `sh-protocol` → WASM wire-parity bridge (`sh-wasm`, `wasm-bindgen`): `CommonHeader`/`VideoHeader`/`InputEvent`/`NackFeedback`/`CodecCaps`/`TransportCaps` + `negotiate` exposed to JS; 41 `#[wasm_bindgen_test]` golden-vector + round-trip + hostile-input tests all pass under `wasm-pack test --node`; `wasm-pack build --target web` succeeds; native workspace unaffected (excluded); CI `wasm` job added. ADR-0019. **Live `RTCPeerConnection`/DataChannel browser client deferred → R-BROWSER-INTEROP (no WebDriver here).** | sh-wasm | P4 | network-engineer | 41 wasm-bindgen-test wire-parity + hostile-input (all ✅ `wasm-pack test --node`) | 🟡 | |
| P5-1b | Browser crypto WASM bridge (`sh-crypto-wasm`): exposes `sh-crypto` device identity, R-SIG-AUTH (`IdentityProof` create/verify), and Noise XK/IK + DTLS binding (P4-5) to browser JS via `wasm-bindgen`. Private Ed25519 signing keys stay in wasm linear memory (never cross JS boundary). WebCrypto CSPRNG (`getrandom/js`) is the sole entropy source, entropy-probed so unavailability is a catchable `JsError` (never a wasm trap). All `sh-crypto` invariants preserved (constant-time verify, zeroize, `verify_strict`, DTLS anti-downgrade gate). 23 `#[wasm_bindgen_test]` all pass (`wasm-pack test --node`): identity entropy + stability, trust store, R-SIG-AUTH roundtrip + cross-target parity (wasm proof → native verify + native proof → wasm verify), full wasm-path XK handshake (`complete_for_first_pairing`) with DTLS binding, anti-downgrade (`ALG=NONE`/zero-commit → JsError; `dtls_pin()` returns None for zero commit), hostile-input tests. `wasm-pack build --target web` succeeds. CI `wasm` job extended (pinned wasm-pack + wasm-target clippy). ADR-0020. Live Noise handshake over real WebSocket+WebRTC deferred → R-BROWSER-CRYPTO-LIVE. | sh-crypto-wasm | P4-5, P5-1 | security-engineer, rust-staff-engineer | 23 wasm-bindgen-test identity+auth+noise (all ✅ `wasm-pack test --node`); cross-target parity proven | 🟡 | |
| P5-1c | Browser WebRTC client (`sh-web-client`): Rust/wasm orchestration of `RTCPeerConnection` with **DTLS-fingerprint identity pinning** — the same MITM defense as native (P4-5/ADR-0014). Extracts the local SDP `a=fingerprint` to commit in the Noise `BindCert`, runs the identity-bound XK handshake, then verifies the **remote** SDP `a=fingerprint` (constant-time, byte-exact) against `require_dtls_pin()` **before** `setRemoteDescription` — a signaling/SDP fingerprint swap aborts the connection (no DTLS over the attacker cert). SDP parsing is hostile-input safe (malformed → `JsError`, never panics/traps; bounded by a 64 KiB `MAX_SDP_BYTES` DoS cap). The MITM gate is **fail-closed**: `guard_remote_sdp` errors when no pin is set (a forgotten handshake can no longer silently skip the check); `create_answer` is `pub(crate)` (not `#[wasm_bindgen]`) so the only answerer entry point is the pin-checked `connect_as_answerer`. No `unwrap/expect/panic` in production; `#![deny(missing_docs)]`. 18 `#[wasm_bindgen_test]` pass in **headless Firefox** (`wasm-pack test --headless --firefox`): 11 SDP-parser hostile-input tests (incl. oversized-SDP cap + valueless-fingerprint-line skip) + 7 WebRTC e2e (2-PC loopback happy path with real XK + DTLS pin + SHP frame round-trip; **non-vacuous** MITM rejection offerer + answerer paths with honest-control-verifies/tamper-rejected; fail-closed no-pin rejection). `wasm-pack build --target web` succeeds; native workspace unaffected (excluded); CI `browser-webrtc-client` job (Firefox + geckodriver pinned `v0.37.0` to match ADR-0021). `parse_sdp_fingerprint` cargo-fuzz target added (`crates/sh-web-client/fuzz`, native libFuzzer; CI wiring → [FUZZ-TODO] R-BROWSER-FUZZ). ADR-0021. Live browser↔**native** e2e still deferred → R-BROWSER-INTEROP; Chrome/Safari matrix → R-BROWSER-MATRIX. | sh-web-client | P4-5, P5-1, P5-1b | network-engineer, security-engineer | 18 wasm-bindgen-test (all ✅ `wasm-pack test --headless --firefox`); non-vacuous MITM rejection (offerer + answerer) + fail-closed no-pin gate proven; `parse_sdp_fingerprint` fuzz target | 🟡 | |
| P5-2 | Browser viewer/control UI + H.264 codec negotiation + input capture (`clients/web`): strict-TypeScript app over the merged wasm bridges (Vite + Vitest + `@playwright/test`/Firefox, pinned devDeps). **VIEW**: inbound SHP video frames are header-decoded by the wasm bridge (`decode_common_header`/`decode_video_header`) and the H.264 Annex-B payload is decoded by WebCodecs `VideoDecoder` (annexb mode, codec string from inline SPS) and painted to a `<canvas>`; **hostile host frames are non-fatal** (malformed → dropped; decode/configure error → caught, decoder torn down, re-primes on the next keyframe — never crashes). **CONTROL**: DOM mouse/keyboard/wheel on the canvas → pure SHP field mapping (coords normalized to `0..=65535` bbox-relative; DOM `code`→USB-HID; button-mask; px×8 wheel) → wasm `encode_input_event` (TS never serializes the wire) → DataChannel. **H.264 negotiation** via `encode_caps`/`decode_caps` (H.264 advertised + selected for the browser), surfaced in the UI. Thin DOM/WebCodecs shell — security-/wire-critical logic stays in the wasm crates (no TS reimplementation); one wasm binary/one init via `sh-web-client`'s re-export superset; strict TS, no `any` on the wire/security path. **Tests:** 60 Vitest unit tests (input DOM-event→SHP **byte-exact** vs the real Rust codec via a `--target nodejs` sh-wasm build, codec negotiation = H.264, SHP frame-header parse hostile-input-safe, coord mapping, Annex-B NAL/SPS/PPS) + a Playwright **headless-Firefox loopback e2e** (real `WebClient` viewer (offerer) ↔ raw `RTCPeerConnection` host (answerer) over a real DTLS DataChannel; host sends a real SHP-wrapped H.264 keyframe → it reaches the viewer's decoder over the wire. **Always-asserted, codec-INDEPENDENT** (catch a transport/control/pipeline regression): loopback connected over real DTLS, H.264 negotiated, malformed frame sent first survives, the frame **reached the decoder input** (`framesReachedDecoder≥1`), and the synthetic canvas click round-trips to the host as the **exact** SHP bytes vs an independent expectation — non-vacuous. **Conditional** decode→pixels (16×16 `VideoFrame` + canvas non-blank) is asserted only where Firefox can WebCodecs-decode H.264 (OpenH264/ffmpeg present: system Firefox locally; CI Firefox when `ffmpeg` installed), and gracefully skipped otherwise so CI stays green — so the pixel-decode is still proven everywhere the codec is present, without overclaiming CI always pixel-decodes; 3× no-flake). Sample keyframe fixture is a real decodable 16×16 H.264 Annex-B **I_PCM** frame constructed per ISO/IEC 14496-10 (no encoder exists in-env; reproducible via `scripts/gen-h264-fixture.mjs`). CI `web-ui` job (wasm build + npm audit + tsc + vite build + vitest + best-effort `ffmpeg` install + Playwright Firefox; Firefox/geckodriver pinned). Native workspace unaffected (TS app + wasm crates outside the cargo workspace). ADR-0022. **Live host↔browser video deferred → R-BROWSER-INTEROP; Chrome best-effort/Safari impossible-on-Linux → R-BROWSER-MATRIX; clipboard/file/audio + UX polish deferred.** | clients/web (sh-wasm/sh-crypto-wasm/sh-web-client) | P5-1, P5-1b, P5-1c | ui-engineer, ux-engineer, security-engineer | 60 Vitest unit tests (byte-exact input map, H.264 negotiation, hostile-frame parse, coords, Annex-B) + 1 Playwright Firefox loopback e2e (real H.264 decode→canvas + control round-trip, non-vacuous, 3× no-flake) | 🟡 | |
| P5-3 | Browser↔native WebRTC interop (Stage 1 — DataChannel echo): `SdpBridgeBuilder` in `sh-transport` (hostile-input-bounded SDP offer acceptance: 64 KiB cap, bounded line scan, per-group hex validation, exactly 32 bytes for SHA-256, always calls `pin_remote_dtls` — DTLS-pin invariant ADR-0017 preserved); `PinnedWebRtcTransport::add_remote_candidate` for trickle ICE; `PinnedWebRtcTransport::add_local_host_candidate` to register the concrete UDP address with str0m ICE (required for connectivity — str0m does not infer the local candidate from `accept_offer`); `streamhaul-signaling` binary (`SignalingServer` + `AcceptAll`, insecure-lan, prints `SIGNALING_READY`); `streamhaul-webrtc-host` binary (binds `127.0.0.1:0`, creates `Rtc`, prints `HOST_DTLS_FP=<64-hex>`, connects to signaling, accepts SDP offer via `SdpBridgeBuilder`, registers local ICE candidate, sends SDP answer + `EndOfCandidates`, runs tokio drive loop via `spawn_webrtc_driver` + `TokioUdpSocket`, pumps trickle ICE, accepts DataChannel, echoes frame + sends SHP echo); `clients/web/src/signaling/envelope.ts` (TS `encodeEnvelope`/`decodeEnvelope` matching the `sh-signaling` wire format exactly; hex-pattern validation on fingerprint fields); `browser-native.ts` (uses `ws://127.0.0.1:8765` to avoid IPv6 ambiguity; resolves on `Answer` not `EndOfCandidates`; session-ID hex-validated); Playwright e2e (`browser-native.{ts,html,spec.ts}`) guarded by `BROWSER_NATIVE_E2E=1`; CI `browser-native-e2e` job (Linux, Firefox + geckodriver pinned ADR-0021/0022 versions); 10 `parse_sdp_fingerprint` unit tests (valid, lowercase-algo, uppercase-algo, no-fingerprint, too-large, wrong-byte-count, invalid-hex, wrong-group-length, oversized-line-skipped, beyond-max-lines, error-display). `anyhow = "1"` added to workspace deps. ADR-0023. **STAGE 2 DONE — identity-bound DTLS pin (the headline MITM defense), ADR-0023 addendum:** the live **Noise XK handshake now rides the signaling channel** (new opaque `MessageKind::Noise = 8`, routed by `(session_id, to_fp)` only — zero-knowledge preserved; payload = `[sub_type]||body`, sub-types `HELLO`/`HOST_STATIC_PUB`/`MSG`). Browser = XK **initiator** (`WasmNoiseHandshake.initiator_xk_with_dtls`, commits its **own** local DTLS `a=fingerprint` from `WebClient.local_dtls_fingerprint()` in the BindCert); native host = XK **responder** (advertises its X25519 static pub so XK can bootstrap; commits its own DTLS fp). Each extracts the OTHER's BindCert-committed fp and **pins THAT, not the SDP**: browser feeds `require_dtls_pin()` → `WebClient.set_dtls_pin()` → `connect_as_offerer` fail-closed `verify_sdp_fingerprint_pin` against the host **answer** SDP; native host pins via the **new** `SdpBridgeBuilder::accept_browser_offer_with_pin(offer, bindcert_fp, …)` (the offer SDP `a=fingerprint` is **ignored** — str0m fail-closes DTLS against the identity-bound pin; ADR-0017 builder pin invariant preserved). TOFU first pairing via `complete_for_first_pairing` (browser) / a native `TrustAllKeystore` (host bin). `WebClient.add_ice_candidate` now sets `sdpMid="0"`/`sdpMLineIndex=0` (Firefox requires it; empty candidate = EOC, ignored). **e2e (the genuine browser↔native MITM proof), headless Firefox, 5×-green:** (a) **identity-bound happy path** — connects + echoes AND asserts `pinUsedHex === host_fp` (the pin is sourced from the Noise BindCert, NOT just any SDP value — non-vacuous); (b) **MITM rejection** — a man-in-the-middle swaps the host's answer SDP `a=fingerprint` after the BindCert committed a different one → `connect_as_offerer` ABORTS via the fail-closed pin gate; the happy-path arm is the honest control (same setup connects when untampered) — the browser↔native equivalent of the native `dtls_identity_binding` MITM test. 2 new `accept_browser_offer_with_pin` unit tests (pins-supplied-not-SDP + oversized-SDP reject). **Deferred (ADR-0023 addendum): R-SIG-AUTH on the live signaling path (the Noise/BindCert layer gives the identity binding; signaling-peer-auth is separable — the e2e stays `insecure-lan`/`AcceptAll`), persistent TOFU pinning across reconnects, media tracks, Chrome/Safari (R-BROWSER-MATRIX), coturn (R-COTURN-DEPLOY).** | sh-transport, bins/streamhaul-signaling, bins/streamhaul-webrtc-host, clients/web, sh-signaling, sh-web-client, sh-crypto-wasm | P4-5 (ADR-0017), P5-1c (signaling wire format), P5-1b (wasm Noise), R-WEBRTC-LIVE (drive loop) | rust-staff-engineer, network-engineer, security-engineer | 12 SdpBridgeBuilder unit tests (10 `parse_sdp_fingerprint` + 2 `accept_browser_offer_with_pin`: pins-supplied-not-SDP, oversized-reject); Playwright browser↔native e2e (Firefox, **identity-bound** connect + echo with `pinUsedHex===host_fp` + **non-vacuous MITM rejection**, `BROWSER_NATIVE_E2E=1`, 5× no-flake) | ✅ | |

**Gate P5:** 🟡 **Firefox** view + control (☐ Chrome/Safari) · ✅ H.264 negotiated for browser (exercised in headless Firefox) · ✅ same relay path as native — **browser↔native identity-bound interop PROVEN** (P5-3 Stage 2): a real Firefox `WebClient` connects to the native `streamhaul-webrtc-host` over the live zero-knowledge `sh-signaling` relay, with the DTLS pin sourced from the **identity-signed Noise BindCert** (not the SDP), and a signaling/SDP MITM is rejected — the same DTLS↔identity binding the native transport enforces (P4-5). *(Chrome/Safari browser-matrix verification stays environment-gated — same posture as Gate P0 hardware items. Wire-parity slice: 🟡 P5-1. Browser crypto bridge: 🔬 P5-1b. Browser WebRTC client with DTLS-pin MITM defense, verified in **live headless Firefox** (real `RTCPeerConnection` loopback, non-vacuous MITM rejection): 🟡 P5-1c. **Browser viewer/control UI + H.264 negotiation: 🟡 P5-2** — VIEW (real H.264 keyframe decoded via WebCodecs → canvas, hostile-frame-safe), CONTROL (canvas input → exact SHP bytes round-tripped to the host), and H.264 negotiation are now **exercised in-browser in headless Firefox** (Vitest pure-logic + Playwright Firefox loopback e2e, CI `web-ui` job). **Browser↔native identity-bound interop: ✅ P5-3 Stage 2** — live Noise XK over `sh-signaling` + DTLS pin from BindCert + non-vacuous MITM rejection, headless Firefox, 5× no-flake, CI `browser-native-e2e`. The remaining Gate ☐ is the **Chrome/Safari matrix** (R-BROWSER-MATRIX). Live-signaling R-SIG-AUTH on the browser↔native path stays deferred (the identity binding is the separable Noise/BindCert layer). ADR-0022/0023.)*

**Risk Register additions (P5-1 / P5-1b):**

| ID | Description | Blocks | Owner |
|----|-------------|--------|-------|
| R-BROWSER-INTEROP | **CLOSED for the identity-bound DataChannel path (P5-3 Stage 2, ADR-0023).** A real Firefox `WebClient` ↔ native `streamhaul-webrtc-host` session now connects over the live zero-knowledge `sh-signaling` relay with a DTLS pin sourced from the **identity-signed Noise BindCert** (XK handshake over `MessageKind::Noise`), and a signaling/SDP-fingerprint MITM is **rejected** (non-vacuous, honest-control arm connects). The native side uses `SdpBridgeBuilder::accept_browser_offer_with_pin` (ADR-0017 pin invariant preserved); the browser uses `sh-web-client`'s fail-closed `verify_sdp_fingerprint_pin`. Headless Firefox, 5× no-flake, CI `browser-native-e2e`. Stage 1 (DataChannel echo, SDP-pinned transport interop) and the intra-browser loopback (P5-1c) remain as earlier-proven sub-steps. **H.264-over-DataChannel transport now PROVEN (ADR-0031):** `streamhaul-webrtc-host --stream-video` streams a **baked** H.264 clip (pre-encoded by the excluded `sh-codec-openh264` crate's `gen_browser_fixture` example; the workspace host links no encoder) as SHP video frames, and the browser DECODES & paints them via WebCodecs — a real browser↔native video path, CI `browser-native-e2e` asserts `framesReachedDecoder>=1` (decode gated on WebCodecs availability since headless Firefox H.264 isn't guaranteed). The host framing is unit-tested to round-trip through the same `sh-protocol` decoders the browser runs. **Live X11 capture + OpenH264 path now component-CI-verified (ADR-0032):** `streamhaul-webrtc-host` is now a **lib + bin** (`VideoFrameSource` trait, `BakedFrameSource`, `HostConfig`, `StreamMode`, `run_webrtc_host` public entry point); the workspace binary behavior is unchanged (still uses the baked fixture). The new workspace-EXCLUDED `streamhaul-webrtc-preview` binary wraps `X11ScreenCapturer → DownscaleCapturer → EvenDimCapturer → LiveFrameSource` and calls `run_webrtc_host` — streaming the *live* X11 screen as real H.264 to the browser tab. CI `webrtc-preview` job (Linux) verifies the full encode+SHP-frame pipeline deterministically (SyntheticCapturer → OpenH264 → `build_shp_video_frame`). Both ADR-0028 isolation invariants are preserved. **SHP fragmentation now DONE (ADR-0033):** `build_shp_video_fragments` splits frames over the 64 KiB `payload_len` cap into multiple SHP messages (shared `frame_id`; `frag_index`/`total_frags`/`marker`); a `VideoFragmentReassembler` in the browser concatenates them in order before decode. Single-fragment output is byte-identical (existing e2e unchanged); the `browser-native` video e2e runs the host with `--max-fragment-bytes 4096` to force + verify reassembly. The downscale is demoted to an optional bandwidth knob — the preview now defaults to **full resolution** (`--max-width 3840`). **Input/control back-channel now DONE (ADR-0034) — it's a real remote DESKTOP, not just a viewer:** the host demuxes browser→host 16-byte `InputEvent`s off the DataChannel (non-blocking `timeout(ZERO, recv())` drain between video frames) and injects them via a `Box<dyn InputInjector>`; the live `streamhaul-webrtc-preview` host injects into real X11 via `XTestInjector` (actual remote control), the workspace host uses a `StdoutInputLogger`. The `browser-native` video e2e now sends synthetic `PointerMove` events and asserts the host's `INPUT_INJECTED` log — proving browser→host control end to end in headless Firefox; host unit tests cover decode+inject + malformed-event rejection. **Periodic keyframes now DONE (ADR-0035):** `OpenH264Encoder::with_config` sets a ~2 s GOP (`intra_frame_period = 2×fps`) so a receiver joining mid-stream or recovering from loss gets an IDR within ~2 s (was: only one IDR ever); `request_keyframe()` still forces extra IDRs on demand. **Stuck-button safety now DONE (ADR-0034 follow-up), platform-complete:** `InputInjector::release_all()` (defaulted no-op) is implemented on **all three OS injectors** — `XTestInjector` (Linux), `CgEventInjector` (macOS), `SendInputInjector` (Windows) each release any button still set in their `prev_button_mask` — and is called by the host injection thread on session end (awaited on **every** exit path, including stream errors), so a disconnect can't leave a mouse button stuck (keys/modifiers are already atomic press+release pairs). The Linux path is verified end-to-end against the X server via `QueryPointer` (no-flake, 40×); mac/win assert the tracked-state bookkeeping on their CI runners (live OS-effect deferred to hardware per R-MAC-TCC / R-WIN-INTERACTIVE). **Hostile-input rate-limiting now DONE (ADR-0034 guard):** the host caps injected **drop-safe high-rate** events with a token-bucket `sh_input::RateLimiter` (500/s sustained, burst 120) — `PointerMove` (absolute position) AND `Wheel` (stateless scroll notch) are throttled, while `Button`/`Key` state transitions always pass (dropping a release would re-stick) and `Touch`/`Pen` pass (contact-state, Unsupported today). `admit_input` classifies by drop-safety (proven by unit test, incl. that a `Wheel` flood can't bypass by relabeling); the limiter takes the clock as a parameter for deterministic tests. **Dedicated low-latency Input channel — host side now DONE (ADR-0036, PR 1):** `run_video_stream` accepts up to two DataChannels and routes by parsed `spec().channel` (`classify_channels`, order-independent); with a dedicated `ChannelId::Input` channel present it runs the video send loop and a **dedicated input task** on separate SCTP streams — input is delivered the instant it arrives (≤1 frame interval → ~1 SCTP RTT) and not head-of-line-blocked by video. A legacy single-channel peer (today's browser, `"shp"` → `Control`) falls back to the between-frames drain, so the existing e2e stays green. The safety-critical concurrent teardown (injection thread fed by **two sender clones**; `select!` → abort-the-loser → `release_all()` fires on **every** exit order) is unit-tested for the input-closes-first, video-completes-first, and video-source-error paths. **Browser side now DONE (ADR-0036, PR 2):** `sh-web-client`'s `create_offer` opens BOTH channels (video `"0:128:1"` + input `"2:0:1"`); the new `send_input` sends events on the dedicated Input channel while `send_frame`/`on_frame` stay on the video (primary) channel (echo/MITM path unaffected — `run_data_channel` routes to the video channel by spec). The `browser-native` video e2e sends input via `send_input`, and because input now arrives ONLY on the Input channel the existing `INPUT_INJECTED` assertion also proves the host took the **dual-channel** path. Live end-to-end in headless Firefox.** **Still deferred (separate risk items):** a **discrete-event aggregate rate cap that preserves releases** (the remaining bounded discrete-flood DoS — security-assessed low severity, bounded by queue depth + per-frame drain + serial injection), full browser↔live-host Playwright e2e with `streamhaul-webrtc-preview`, *mid-session* input-loss gap detection (needs input sequence numbers), clipboard, NACK/PLI-driven keyframes, Chrome/Safari (R-BROWSER-MATRIX), live coturn (R-COTURN-DEPLOY), R-SIG-AUTH on the live signaling path (R-SIG-AUTH-LIVE). ADR-0019/0021/0023/0031/0032/0033/0034/0035/0036. | media/UI on live native path, R-BROWSER-MATRIX | network-engineer, ui-engineer |
| R-BROWSER-MATRIX | **Chrome / Firefox / Safari compatibility matrix is deferred from P5-2.** P5-1c verifies the WebRTC DTLS-pin client in **headless Firefox only** (its `webdriver.json` enables Firefox-specific `media.peerconnection.ice.loopback`; Chrome/Safari need different prefs and drivers). The client must still be verified across all three browsers including Safari/WKWebView constraints (no AV1 encode, H.264 MSE profile restrictions). Requires the three-browser CI matrix from P5-2. ADR-0019/0021. | P5-2 Gate P5 | ui-engineer, qa-engineer |
| R-BROWSER-FUZZ | **[FUZZ-TODO] Wire the `sh-web-client` `parse_sdp_fingerprint` cargo-fuzz target into a CI/nightly run.** The libFuzzer target exists (`crates/sh-web-client/fuzz/fuzz_targets/parse_sdp_fingerprint.rs`) and compiles/runs natively (`cargo +nightly fuzz run parse_sdp_fingerprint`) — the SDP parser is a parser of untrusted signaling bytes, so CLAUDE.md §5 makes a fuzz target mandatory. What remains: provisioning a nightly cargo-fuzz toolchain on the runners and adding a short timed fuzz job (same posture as R-ICE-FUZZ for `stun_decode`/`turn_decode`). The parser is already strict/bounded (64 KiB cap, exact-32-byte decode, never indexes out of bounds) and covered by hostile-input unit tests; continuous fuzzing is the defense-in-depth follow-up. | X-2 / GA | qa-engineer, security-engineer |
| R-BROWSER-FILE | **Live browser↔native file transfer over a real DataChannel is deferred from P7-2 (ADR-0024).** The web file module (`clients/web/src/file/`) is complete and verified **in-process** (sender→receiver over the validated TS state machines, 91 Vitest + 60 wasm-pack tests, byte-parity with the Rust codec, WebCrypto SHA-256 integrity). What remains: wiring `FileTransferSender`/`FileTransferReceiver` to a live `RTCDataChannel` against the native `streamhaul-webrtc-host`, plus a Playwright e2e — the same WebDriver/live-media environment already gated for R-BROWSER-INTEROP / R-BROWSER-MATRIX (the P5-2 line notes "clipboard/file/audio deferred"). Also: the **save-sink caller obligation** (sanitized name only, OS-specific reserved-name filtering e.g. Windows `CON`/`NUL`) is documented in `transfer.ts` + `sh-core::file` and partially honored (download uses the sanitized name); OS-reserved-name filtering lands with the platform thin shim. The orchestrator + framing security (capability gate, bounds, integrity, name sanitization) are fully gated and merged. ADR-0024. | media/UI on live native path, R-BROWSER-MATRIX | ui-engineer, network-engineer, qa-engineer |
| R-BROWSER-CRYPTO-LIVE | **Live Noise XK/IK handshake over real WebSocket + WebRTC is deferred from P5-1b.** The `WasmNoiseHandshake` / `WasmKeystore` / R-SIG-AUTH bridge is complete and verified in-process (`wasm-pack test --node`, 23 tests, cross-target parity proven). Wiring `WasmNoiseHandshake` into a live browser session — `window.WebSocket` → `sh-signaling` → `RTCPeerConnection`, calling `require_dtls_pin()` before transport setup — requires WebDriver and is deferred to P5-2. **Type-enforcement follow-up:** make `complete_for_first_pairing` return a distinct first-pairing outcome that cannot open a session without an explicit `trust_peer_by_key` pin (mirroring ADR-0017's `PinnedWebRtcTransport`), so reconnect-misuse is a compile error — lands with the live-wiring PR. ADR-0020. | P5-1b live, P5-2 | security-engineer, network-engineer |
| R-SIG-AUTH-LIVE | **R-SIG-AUTH on the live browser↔native signaling path is deferred (P5-3 Stage 2, ADR-0023).** The browser↔native e2e uses the `insecure-lan` / `AcceptAll` signaling authenticator (empty `Hello` proof). This is **independent of the identity binding proven in Stage 2**: the DTLS↔device-identity binding comes from the Noise/BindCert layer (each peer commits its DTLS `a=fingerprint` inside an Ed25519-signed `BindCert`, verified inside the Noise transcript), which holds regardless of whether the signaling channel authenticates the peer. Wiring the production `IdentityProofAuthenticator` (ADR-0016, already implemented + tested natively) into the live browser↔native path — browser computes `create_identity_proof` over the server `Challenge` and the host verifies — is a separable hardening step deferred here. Until then, do not run `streamhaul-webrtc-host`/`streamhaul-signaling` outside local test infra (the `insecure-lan` `compile_error!` release fence enforces this). ADR-0016/0023. | GA | security-engineer, network-engineer |
| R-BROWSER-CRYPTO-MATRIX | **wasm-pack test --node verifies V8 only.** Firefox SpiderMonkey and Safari JavaScriptCore WASM engines may behave differently; cross-browser crypto compat requires the three-browser CI matrix from P5-2. ADR-0020. | P5-2 Gate P5 | qa-engineer |

**Risk Register additions (P6-2 — Linux host, ADR-0025):**

| ID | Description | Blocks | Owner |
|----|-------------|--------|-------|
| R-LINUX-WAYLAND | **Wayland capture + input deferred from P6-2.** The Linux host ships **X11-only** (`X11ScreenCapturer` + `XTestInjector`, verified under Xvfb). A Wayland session has no XTEST/GetImage; capture needs **PipeWire ScreenCast via the xdg-desktop-portal** (with a user consent dialog) and input needs **`libei`** (or `/dev/uinput`, see R-LINUX-UINPUT). The dev/CI environment has no Wayland session/portal, so this is environment-gated. Touch/Pen injection (XI2) folds in here. ADR-0025. | GA Linux desktop coverage | realtime-systems-engineer, ui-engineer |
| R-LINUX-VAAPI | **VA-API hardware H.264/HEVC encode deferred from P6-2.** `libva`/`libva-drm` are present and the box has a render node (`/dev/dri/renderD128`), but no VA-API driver is confirmed (`vainfo` absent), so a HW-encode path cannot be verified here. The portable software encode path in `sh-codec-hw` remains the fallback; the capturer already emits encoder-friendly `Bgra8` (and can be extended to NV12). ADR-0025. | on-hardware HW-encode validation | realtime-systems-engineer, performance-tuning-engineer |
| R-LINUX-UINPUT | **`/dev/uinput` injection backend deferred from P6-2.** XTEST is the correct path for an **X11** session (no elevated permission, injects through the X server); `/dev/uinput` is the **Wayland/headless** backend and the device is root-only in this environment. The `uinput` backend (and a Wayland `libei` backend) land with R-LINUX-WAYLAND. ADR-0025. | Wayland/headless input | realtime-systems-engineer |
| R-LINUX-AUDIO | **PipeWire audio capture deferred from P6-2.** The PipeWire socket is present, but host audio capture is a separate, heavyweight integration with no consumer wired yet; deferred to keep P6-2 focused on the verifiable video+input slice. ADR-0025. | host audio on Linux | realtime-systems-engineer |

**Risk Register additions (P6-1 — macOS host, ADR-0026):**

| ID | Description | Blocks | Owner |
|----|-------------|--------|-------|
| R-MAC-TCC | **Live macOS capture + injection are TCC-gated and verified on hardware, not CI.** macOS gates `CGDisplay::image()` behind **Screen Recording** and `CGEventPost` behind **Accessibility** (TCC), which cannot be granted on a headless `macos-latest` runner. So CI **compiles** `sh-platform-mac` against the real CoreGraphics SDK + unit-tests the pure keymap + runs construction/dispatch smoke tests, but live capture/inject (real pixels/keystrokes) and the **TCC permission-request flow** must be exercised on a real Mac before shipping. Without permission, capture returns a typed error and injection is a silent no-op (fail-soft, never a crash). ADR-0026. | shipping the macOS host | mobile-engineer, qa-engineer |
| R-MAC-SCK | **ScreenCaptureKit + VideoToolbox HW encode deferred from P6-1.** The first slice uses CoreGraphics `CGDisplay::image()` (deprecated-but-functional in macOS 14+) for capture and reuses the portable encode path. The modern, non-deprecated capture API is **ScreenCaptureKit** (async `SCStream`) and HW encode is **VideoToolbox**; both are follow-ups (harder to bring up compile-only without a local macOS toolchain). ADR-0026. | modern capture + HW encode on macOS | realtime-systems-engineer, mobile-engineer |
| R-MAC-SCROLL | **Scroll-wheel injection deferred from P6-1.** `EventType::Wheel` returns `Unsupported` (a typed refusal, not a silent drop); the CoreGraphics scroll-wheel event (`CGEventCreateScrollWheelEvent`) binding lands as a follow-up. ADR-0026. | macOS scroll input | mobile-engineer |
| R-MAC-AUDIO | **Core Audio capture deferred from P6-1.** Host audio on macOS (Core Audio / a virtual aggregate device) is a separate integration with no consumer wired yet; deferred to keep P6-1 focused on the compile-verifiable video+input slice. ADR-0026. | host audio on macOS | realtime-systems-engineer |

**Risk Register additions (P6-Windows — Windows host, ADR-0027):**

| ID | Description | Blocks | Owner |
|----|-------------|--------|-------|
| R-WIN-DXGI | **DXGI Desktop Duplication + multi-monitor deferred from the Windows host.** The first slice uses GDI `BitBlt`+`GetDIBits` (copies the framebuffer; **primary monitor only** — the injector pins the pointer to the same primary-monitor space, no `VIRTUALDESK`, so multi-monitor stays aligned). The low-latency zero-copy path is **DXGI Desktop Duplication** (via the `windows` crate), which also enables full **virtual-desktop / per-monitor** capture. ADR-0027. | low-latency + multi-monitor Windows capture | realtime-systems-engineer |
| R-WIN-INTERACTIVE | **Full interactive capture/inject validation on a real Windows desktop deferred.** CI compiles `sh-platform-win` against the real Win32 SDK and runs the GDI/`SendInput` smoke tests on the `windows-latest` interactive desktop (asserting panic-freedom + structural validity, not on-screen effect). Full validation — real captured pixels, real injected keystrokes/clicks, and the **UIPI / UAC secure-desktop** limitations (`SendInput` cannot target higher-integrity windows or the secure desktop) — is a real-desktop follow-up. **Caller obligation (load-bearing):** Windows has **no per-app OS gate** within a session (no TCC equivalent), so the upstream `Capabilities::CONTROL`/`VIEW` check is the **sole** authorization gate — the host pipeline MUST enforce it before constructing/feeding the injector/capturer. ADR-0027. | shipping the Windows host | qa-engineer, security-engineer |
| R-WIN-AUDIO | **WASAPI loopback audio capture deferred from the Windows host.** Host audio on Windows (WASAPI loopback) is a separate integration with no consumer wired yet; deferred to keep the slice focused on the compile-verifiable video+input. ADR-0027. | host audio on Windows | realtime-systems-engineer |

---

## Phase 6 — Cross-OS hosts

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P6-1 | `sh-platform-mac`: macOS host capture + input injection. **CoreGraphics SLICE DONE, compile-CI-verified** (ADR-0026): `CgDisplayCapturer` (impl `sh_media::ScreenCapturer` via `CGDisplay::image()` → stride-repacked `Bgra8` `VideoFrame`; 32bpp + length validated; `None` without Screen Recording → typed error, never a panic) + `CgEventInjector` (impl `sh_input::InputInjector` via `CGEvent`: pointer move through `CoordMapper`, button press/release mask-diff, key via a USB-HID→macOS-virtual-keycode subset + `CGEventFlags` modifiers; scroll/touch/pen + unknown key → `Unsupported`, never a wild injection; fail-closed `new()`). Hostile-input-safe (no `unwrap/panic/unsafe`; §7 — never logs keystrokes/coords/pixels). The pure `keymap` (HID→keycode) is unit-tested on **every** platform (all 76 entries verified vs `kVK_*`). **"CI as the compiler":** `core-graphics`/`core-foundation` deps target-gated to macOS; the new **`platform-mac`** CI job (macos-latest) + the `test (macos-latest)` workspace job compile it against the real CoreGraphics SDK with clippy `-D warnings`, and run keymap + macOS construction/dispatch smoke tests. bug-bot clean · security-engineer promotable · code-reviewer findings addressed. **Deferred to real hardware / follow-ups (TCC can't be granted headlessly):** live capture+inject + permission flow (R-MAC-TCC), ScreenCaptureKit + VideoToolbox HW encode (R-MAC-SCK — `CGDisplayCreateImage` is deprecated-but-functional), scroll-wheel (R-MAC-SCROLL), Core Audio (R-MAC-AUDIO). | sh-platform-mac | P2 | mobile-engineer, realtime-systems-engineer, security-engineer | macOS capture/inject smoke (compile-CI-verified on macos-latest; keymap tested everywhere) | 🟡 | #56 |
| P6-2 | `sh-platform-linux`: Linux host capture + input injection. **X11 SLICE DONE** (ADR-0025): `X11ScreenCapturer` (impl `sh_media::ScreenCapturer` via x11rb `GetImage(ZPixmap)` → tightly-packed `Bgra8` `VideoFrame`; depth-24/32 + exact-length validation, never panics on a bad reply) + `XTestInjector` (impl `sh_input::InputInjector` via the XTEST extension: pointer move through `CoordMapper`, button press/release mask-diff, wheel→buttons 4/5/6/7, key via a USB-HID→keysym→keycode subset; unknown key/touch/pen → `Unsupported`, never a wild injection; fail-closed if no display / no XTEST). Treats every network `InputEvent` field as hostile (bounded, no `unwrap/panic/unsafe`); hostile-X-server `keysyms_per_keycode=0` is a typed error, not a `chunks(0)` panic; §7 — never logs keystroke values / pointer coords / pixels. **Verified headlessly in CI under Xvfb** (real X server): pointer round-trip (`inject`→`QueryPointer`), capture geometry/length, button/wheel/key, fail-closed — 23 unit/integration + 3 doc-tests, gated to skip cleanly with no `$DISPLAY`. bug-bot clean (CRITICAL panic fixed) · security-engineer promotable · code-reviewer findings addressed. CI job `platform-linux` (Xvfb). **Deferred (env/HW-gated):** PipeWire/DRM Wayland capture (R-LINUX-WAYLAND — no Wayland session), VA-API HW encode (R-LINUX-VAAPI — no driver), `/dev/uinput` inject (R-LINUX-UINPUT — root-only device; XTEST is the X11-native path), PipeWire audio (R-LINUX-AUDIO), MIT-SHM zero-copy + full HID map. | sh-platform-linux | P2 | realtime-systems-engineer, rust-staff-engineer, security-engineer | Linux X11 capture/inject smoke (pointer round-trip + capture, Xvfb-verified in CI) | 🟡 | #55 |
| P6-W | `sh-platform-win`: Windows host capture + input injection. **GDI/SendInput SLICE DONE, compile-CI-verified** (ADR-0027): `GdiScreenCapturer` (impl `sh_media::ScreenCapturer` via GDI `BitBlt`+`GetDIBits` top-down 32-bpp → primary-monitor `Bgra8` `VideoFrame`; all GDI handles freed on every path via an RAII guard, GetDIBits called on a deselected bitmap per the Win32 contract) + `SendInputInjector` (impl `sh_input::InputInjector` via `SendInput`: pointer move maps **1:1** — our `0..=65535` coords are exactly `MOUSEEVENTF_ABSOLUTE` on the primary monitor; button mask-diff, wheel `±WHEEL_DELTA`, key via USB-HID→Win32-VK subset + modifier VKs; touch/pen + unknown key → `Unsupported`, never a wild injection). Hostile-input-safe (no `unwrap/panic`; §7 — never logs keystrokes/coords/pixels). The pure `keymap` (HID→VK) is unit-tested on **every** platform. **Contains `unsafe` Win32 FFI** (unlike the safe Linux/macOS bindings) — each block has a `// SAFETY:` justification + RAII no-leak handle cleanup; rust-staff-engineer-authored/reviewed (§6). **"CI as the compiler":** `winapi` target-gated; the new **`platform-win`** CI job (windows-latest) + the `test (windows-latest)` workspace job compile against the real Win32 SDK with clippy `-D warnings` and run keymap + GDI/SendInput smoke tests (Windows has no TCC gate + an interactive runner desktop, so these may exercise capture/inject end-to-end). bug-bot clean (2 MEDIUM fixed: GetDIBits-while-selected, primary-vs-virtual-desktop coord mismatch) · security-engineer promotable · code-reviewer addressed. **Caller obligation:** Windows has **no OS permission gate**, so upstream `Capabilities::CONTROL`/`VIEW` is the *sole* gate (R-WIN-INTERACTIVE). **Deferred:** DXGI zero-copy + multi-monitor (R-WIN-DXGI), full real-desktop + UIPI/UAC validation (R-WIN-INTERACTIVE), WASAPI audio (R-WIN-AUDIO). | sh-platform-win | P2 | rust-staff-engineer, realtime-systems-engineer, security-engineer | Windows GDI/SendInput smoke (compile-CI-verified on windows-latest; keymap tested everywhere) | 🟡 | #57 |
| P6-3 | Cross-OS host↔client interop matrix (all 3 hosts × all clients) | CI | P6-1,P6-2 | qa-engineer | matrix CI job | ☐ | |

**Gate P6:** 🟡 **all 3 OSes capture→frame — host crate for each OS now in the tree** (**Linux X11 → `Bgra8`, runtime-verified under Xvfb in CI, P6-2**; **macOS → `Bgra8`, compile-CI-verified on macos-latest, P6-1** — live capture TCC-gated → R-MAC-TCC; **Windows → `Bgra8`, compile-CI-verified on windows-latest, P6-W** — interactive validation → R-WIN-INTERACTIVE; Wayland/VA-API/SCK-VideoToolbox/DXGI zero-copy deferred — R-LINUX-WAYLAND/R-LINUX-VAAPI/R-MAC-SCK/R-WIN-DXGI) · 🟡 permission flows handled (capture/inject gated by `Capabilities::VIEW`/`CONTROL` at the host-pipeline call site — the platform crates are fail-closed and capability-agnostic by design; macOS adds a second non-bypassable **TCC** gate; **Windows has no OS gate so the capability check is the *sole* gate** — ADR-0025/0026/0027; OS consent/portal *request* flows are R-MAC-TCC / Wayland-portal / R-WIN-INTERACTIVE) · ☐ host↔client matrix green (P6-3). *(All three host crates (Linux X11, macOS CoreGraphics, Windows GDI) implement the same `ScreenCapturer`/`InputInjector` seams: Linux is runtime-verified (Xvfb); macOS + Windows are compile-CI-verified on their real SDKs with live behaviour deferred to hardware. The cross-OS interop matrix (P6-3) and the encode/zero-copy/Wayland follow-ups remain. ADR-0025/0026/0027.)*

---

## Phase 7 — File transfer (congestion-isolated)

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P7-1 | File-transfer channel (own QUIC stream / WebRTC DC, congestion-isolated) + protocol framing. **DONE:** `sh-protocol::file` wire format (ADR-0024) — control plane `FileOffer`/`FileAccept`/`FileAbort`/`FileComplete` (new `KIND_FILE_*` on the reliable Control channel) + a 21-byte `FileChunkHeader` data plane on a dedicated `ChannelSpec::file()` stream; all decoders hostile-input-safe (bounded `MAX_FILE_CHUNK=1 MiB` / `MAX_FILE_NAME=255`, reserved-bit + range rejection, never panic; golden-vector + proptest round-trip + `decode_never_panics`; wired into the `shp_decode` fuzz target). `sh-adaptive::TokenBucket` budget pacer (deterministic, `Duration`-driven) consuming the allocator's File **leftover**. **QoE-under-transfer gate test** (`sh-adaptive/tests/qoe_under_transfer.rs`): a deterministic shared-FIFO-bottleneck simulation — the rigorous proof the old loopback isolation test explicitly deferred — shows paced file keeps **100%** of video frames within a 33 ms deadline while the **greedy (unpaced) honest-control** lands only **0.8%**, proving pacing is decisive (non-vacuous), and the paced file stays within its leftover budget. `sh-transport/tests/file_channel.rs`: a real ~1 MiB multi-chunk file streams over a real QUIC `ChannelSpec::file()` stream and reassembles **byte-identically**. | sh-protocol, sh-transport, sh-adaptive | P2,P4 | network-engineer, rust-staff-engineer, security-engineer | QoE-under-transfer sim (paced 100% vs greedy 0.8%) + real-QUIC byte-identical reassembly + framing golden/proptest/fuzz | ✅ | |
| P7-2 | Resumable transfer + integrity (hash) + client UI. **ORCHESTRATOR DONE** (`sh-core::file`, ADR-0024): `FileSender`/`FileReceiver` — two side-agnostic state machines (unit-testable with no transport). Resume via byte offset (receiver seeds its hash with the retained prefix so integrity covers the whole file; a wrong-but-right-length prefix fails the digest); whole-file **SHA-256** integrity. Enforces all seven carry-forward requirements (`Capabilities::FILE` at the receive entry point, `resume_offset ≤ total_size`, contiguous/monotonic/non-overlapping `offset+len ≤ total_size`, `LAST`-ends-at-EOF cross-check, aggregate buffer cap, name sanitization incl. control-chars + path-traversal, untrusted `transfer_id`). Hand-written `Debug` never prints buffered content (§7). 18 unit tests + a **real-QUIC e2e** (full + resumed, `FileComplete` verdict round-tripped over Control). bug-bot clean · security-engineer promotable · code-reviewer findings addressed. **CLIENT UI DONE**: `clients/web/src/file/` (framing.ts/transfer.ts/ui.ts) — thin TS shell over new `sh-wasm` `encode_/decode_file_*` bindings (offer/chunk-header/accept/complete); the wire bytes stay in Rust/wasm. `FileTransferReceiver` mirrors every `sh-core` validation rule (name sanitization incl. control-chars/path-traversal/non-UTF-8, aggregate cap, contiguous `offset==received_len`, `offset+len≤total_size`, `LAST`-at-EOF, whole-file SHA-256 via WebCrypto + resume). Verified bytes are delivered as a browser download using **only** the sanitized name; `finish()` returns an independent copy; decode-side u64 fields are bounded to the JS safe-integer ceiling (symmetric with encode). Minimal drag-drop send + receive-progress + integrity-OK/failed UI wired into the app shell. **60** `wasm-pack --node` parity/hostile-input tests + **91** Vitest tests (byte-parity, receiver-validation incl. truncated/non-LAST-EOF, in-process round-trip incl. resume + finish-returns-copy); `bridge-conformance` lists the new exports. Both Rust + web halves through the full **bug-bot + code-reviewer + security-engineer** gate (two rounds; web findings — buffer-aliasing, save-sink delivery, decode safe-integer guard, UI disposer/error-handling — all fixed). live browser↔native file e2e deferred → R-BROWSER-FILE. | sh-core, clients | P7-1 | rust-staff-engineer, ui-engineer, security-engineer | resume + integrity test (✅ orchestrator + ✅ web byte-parity/round-trip) | ✅ | #54 |

**Gate P7:** ✅ large transfer doesn't degrade video QoE (P7-1: deterministic shared-bottleneck sim — paced file holds 100% of video frames on-deadline vs 0.8% for the greedy control; budget-isolated via the allocator File-leftover `TokenBucket` **and** structurally via the per-transfer QUIC stream's own flow-control window) · ✅ resumable (P7-2 orchestrator: byte-offset resume proven in unit + real-QUIC e2e — sender resumes at the receiver's prefix length, whole-file hash still verifies) · ✅ integrity-verified (P7-2 orchestrator: whole-file SHA-256 checked before delivery; tampered/truncated/mismatched-resume all rejected). *(All three Gate P7 properties proven. P7-2's client UI — the `clients/web/src/file/` module (thin TS shell over the `sh-wasm` file-framing bindings) + minimal drag-drop send / receive-progress / integrity-indicator — is now DONE, with Vitest byte-parity + in-process round-trip (incl. resume) tests and `wasm-pack --node` parity/hostile-input coverage; the live browser↔native file e2e remains environment-deferred → R-BROWSER-FILE, consistent with the existing browser-matrix posture. ADR-0024.)*

---

## Phase 8 — Native QUIC promotion + mobile

| ID | Task | Crates | Depends | Agent | Tests | Status | PR |
|----|------|--------|---------|-------|-------|:------:|----|
| P8-1 | QUIC+ICE wiring; native↔native auto-selects QUIC; **connection migration** (Wi-Fi↔cellular) | sh-transport, sh-ice | P4 | network-engineer | migration test | ☐ | |
| P8-2 | `sh-ffi` (UniFFI) thin clients for iOS/Android (view + touch→pointer/gamepad) | sh-ffi | P4 | mobile-engineer | device smoke | ☐ | |

**Gate P8:** ☐ native peers auto-select QUIC · ☐ survives network change · ☐ mobile thin clients view+control (WebRTC fallback intact).

---

## Definition of Done (every task PR — mirrors CLAUDE.md §10)

- [ ] Branch + PR title follow Conventional Commits; scope is one task
- [ ] Tests written/updated; full suite green on Linux/Windows/macOS in CI
- [ ] `cargo fmt` + `cargo clippy --all-targets --all-features -- -D warnings` clean
- [ ] **`bug-bot` run on the diff — all confirmed issues fixed**
- [ ] **`code-reviewer` run on the diff — all findings addressed**
- [ ] Crypto/auth/transport? → `security-engineer` reviewed; `cargo audit` clean
- [ ] Public APIs documented (rustdoc); ADR added/updated if a decision was made
- [ ] No `unwrap/expect/panic` in production paths; no new `unsafe` without `// SAFETY:`
- [ ] Coverage not reduced; **this tracker updated** (task → `✅`, PR linked, gate updated)

---

## Risk Register / Open Decisions (from LLD §9 — resolve before they block)

| # | Item | Blocks | Owner |
|---|------|:------:|-------|
| R1 | Pin concrete Noise pattern names (SHA-256 hash per ADR-0005) | P3 | security-engineer |
| R2 | Normalize platform-attestation envelope (TPM quote / App Attest / Play Integrity) | P3/P8 | security-engineer |
| R3 | UGC max lifetime per compliance tier (HIPAA/PCI ≤7d) + escrow quorum schema | P3 | security-engineer |
| R4 | `snow` is unaudited — schedule a crypto review before GA | GA | security-engineer |
| R5 | `str0m` ↔ Safari WebRTC interop validation | P4/P5 | network-engineer |
| R6 | **NVENC 4:2:0↔4:4:4 hardware reconfigure deferred.** P2-4 delivers the fully portable double-buffer orchestration (`SessionLimiter`, `EncoderFactory` seam, `DoubleBufferedEncoder` swap logic) tested against `RawEncoder`. The real NVENC pixel-format reconfigure (changing chroma subsampling on a live NVENC session, which may require `NV_ENC_RECONFIGURE_PARAMS` or a full session teardown depending on driver version) is deferred to the on-hardware session (no NVIDIA GPU / Windows NVENC SDK on the dev box). The `EncoderFactory` seam is designed so the NVENC backend slots in without touching the orchestration. Note: consumer GeForce NVENC limits concurrent sessions to 3–5; the `SessionLimiter` default of 4 leaves one slot for the pipeline; the double-buffer overlap requires max ≥ 2. Verify driver-level behavior (does `NV_ENC_RECONFIGURE_PARAMS` permit format change, or does it require destroy+recreate?) on the target hardware before landing the NVENC backend. | P2/P6 | realtime-systems-engineer |
| R7 | Multi-GPU cross-adapter copy budget on target laptop SKUs | P0/P2 | realtime-systems-engineer |
| R8 | Remove `sh-transport`'s `insecure-lan` path (self-signed + skip-verify TLS) when real crypto lands — delete the module or move to a dev-only testkit crate. Meanwhile it is fenced by a non-default feature, an `InsecureLanLab` witness, and a `compile_error!` that blocks `--release --features insecure-lan`. | P4 | security-engineer |
| R9 | Lab bins (`streamhaul-host`/`streamhaul-client`) report **QUIC RTT**, not true one-way glass-to-photon latency — cross-machine one-way latency needs synchronized clocks (NTP/PTP). Add real one-way latency measurement once clock sync is available. | P0-10 / LAN | performance-tuning-engineer |
| R10 | The bins have no **client-done back-channel**: the host waits a fixed 1.5s drain `sleep` before dropping the connection (a hack). Replace with a proper completion handshake (like the loopback harness's oneshot) so the tail isn't lost and exit is deterministic. | P1 | rust-staff-engineer |
| R11 | Add a `--json` report mode to the lab bins so WiFi/LAN test runs are machine-parseable (automation, regression tracking). | P0-10 | rust-staff-engineer |
| R12 | `AudioCodec` has only `RawPcm` today, so `RawAudioDecoder`'s wrong-codec rejection guard is structurally untestable (`decode_rejects_wrong_codec` is `#[ignore]`d). Re-enable the test when a second variant (Opus) lands. | P2 (Opus) | realtime-systems-engineer |
| R13 | P1-4 ships the **portable audio path only** (synthetic source + raw-PCM codec + `AvSync`). Real **WASAPI loopback capture** + **Opus** encode/decode are deferred until platform crates land (no audio hardware / Opus toolchain on dev box). Trait seams (`AudioEncoder`/`AudioDecoder`/`AudioCodec`) are designed for drop-in Opus. | P2 | realtime-systems-engineer |
| R14 | P1-3 ships the **portable input-injection slice only** (`sh-input`: `InputInjector` trait + `CoordMapper` + `NoopInjector` + `RecordingInjector`). Real platform injection — **Windows `SendInput`/Raw Input**, **Linux `uinput`**, **macOS `CGEvent`** — is deferred to `sh-platform-*` crates (no injection hardware / OS SDKs on the dev box). The `InputInjector` trait is object-safe and designed so platform crates drop in without touching callers. | P2 | realtime-systems-engineer |
| R-FEC | **FEC symbol codec deferred.** P2-6 delivers the adaptive `FecPolicy` ratio engine and `NackFeedback` wire framing. The actual Reed-Solomon / XOR FEC symbol encode/decode codec (generation and reconstruction of repair symbols from the packet stream) is deferred to a follow-up task. It requires its own fuzz-heavy parser of untrusted repair-symbol bytes (a full cargo-fuzz target), significant bitmath, and possibly an external crate (`aes-gcm-siv`-based XOR or `reed-solomon-erasure`). The FEC channel budget and ratio signalling are already wired; the codec slots in via the existing seam without API changes. | P2/P3 | rust-staff-engineer, security-engineer |
| R-CODEC | **Hardware encoder backends deferred (P2-5); a software H.264 encoder now exists (ADR-0028).** The `CodecNegotiator` ladder, `CodecCapabilities` model, `BuildFlavor` OSS/Commercial flag, and `CodecCapsPayload` wire format are implemented + tested. **DONE:** `sh_codec_openh264::OpenH264Encoder` — a software H.264 encoder (`VideoEncoder`) via OpenH264, **the workspace-excluded crate `sh-codec-openh264`** (licensing-gated, same posture as `hevc` — Cisco's royalty-free grant covers only their pre-built binary, not from-source; ADR-0028/0004). Round-trip CI-verified (encode BGRA→Annex-B→decode) in the dedicated `codec-openh264` job. **DONE (ADR-0029): pipeline wiring** — `OpenH264Encoder::with_config` maps the `RateAllocator` video budget (`target_bitrate_kbps`/`target_fps`) onto OpenH264; `openh264_encoder_factory()` slots it into the existing `ModeSwitchEncoder`/`EncoderFactory` seam (where NVENC will go); `OpenH264Decoder` (`VideoDecoder`, Annex-B→BGRA) added; a **deterministic** `encode → sh_core::fragment → Reassembler → decode` integration test proves OpenH264's real (multi-NAL, fragmented) output survives the host→wire→client packetization path. **DONE (ADR-0030): real-screen preview slice** — the workspace-excluded `streamhaul-preview` crate (`streamhaul-preview-host`/`-client`) captures the real X11 screen (`EvenDimCapturer` crops to even dims), OpenH264-encodes at the configured bitrate, streams over QUIC, and decodes back; verified by a deterministic synthetic→OpenH264→real-QUIC→decode loopback test plus an Xvfb real-X11 smoke in the dedicated `preview` CI job. Rate control corrected here: OpenH264 needs frame-skip ON to honor a bitrate target, so `with_config` enables skip only in bitrate mode (skip + pipeline backpressure are complementary drop layers; ADR-0029). **Still deferred:** HW `VideoEncoder`/`VideoDecoder` for AV1 (NVENC/VA-API AV1) + HEVC (VideoToolbox/VA-API/Media Foundation) — need OS-specific GPU SDKs (no GPU/driver on the dev box); they slot behind the P2-4 `EncoderFactory` seam with no negotiation changes. Also pending: the browser↔live-host session glue that invokes this factory over the WebRTC host (kept out of the default `streamhaul-host` so the OSS build links no H.264 encoder — ADR-0028/0029), periodic keyframes + P2 loss recovery, and the `rav1e`/AV1 royalty-free alternative. | P6 / GA | realtime-systems-engineer |
| R-PLATFORM-ATTEST | **`BindCert` platform-attestation field accepted but not verified (P3-2).** The `PLATFORM_ATTEST` field in the `BindCert` TBS is defined in ADR-0007 §2 and carries a platform-specific attestation blob (TPM 2.0 quote, Apple App Attest, Play Integrity token, or empty). P3-2 accepts and round-trips the field structurally (parse, length-check, encode) but does not verify its semantic content. Verification requires OS-specific SDKs and a chain of trust to a known root (e.g. Apple's App Attest CA, Google's Play Integrity root). Until verification is implemented: (a) never treat `PLATFORM_ATTEST` as proof of hardware key custody — it is cosmetic; (b) do not advertise platform attestation in the product until R-PLATFORM-ATTEST is resolved; (c) the field is designed for zero-API-change drop-in verification. **Do not ship GA without platform attestation verification for clients advertising hardware-backed keys.** | P3/GA | security-engineer, rust-staff-engineer |
| R-HW-KS | **Hardware-non-exportable device identity key deferred (P3-1).** The LLD (§6.2, §6.3) specifies that the device identity Ed25519 key must be hardware-non-exportable (TPM 2.0 on Windows/Linux, Secure Enclave / Keychain on macOS/iOS, DPAPI on Windows, Android StrongBox). `SoftwareKeystore` (delivered in P3-1) stores the signing key in ordinary heap memory protected by `zeroize`-on-drop, but cannot prevent a root-level attacker from reading it. Hardware keystores are deferred because: (a) no hardware TPM / SE available on the dev machine, (b) each platform requires a distinct OS-SDK integration (`tpm2-tss` on Linux, `CryptoKit`/`Keychain Services` on Apple, `NCryptCreatePersistedKey`/`BCryptImportKeyPair` on Windows, `android.security.keystore` JNI on Android). The `Keystore` trait is designed so hardware backends drop in without API changes. **Do not use `SoftwareKeystore` in GA builds until a hardware backend is available and wired.** **Sticky revocation hardening note (security-engineer, P3+ gate):** `SoftwareKeystore` permits re-trust after revocation for the factory-reset/re-pair scenario (see ADR 0006). Production / hardware keystore implementations MUST make revocation sticky: once an identity is revoked, re-establishing trust must require a distinct, explicitly operator-confirmed action — not the ordinary first-pairing `trust_peer` call. The P3-3 pairing layer MUST surface any implicit re-trust-after-revoke to the operator before executing it. The `Keystore` trait signature (`trust_peer -> Result<()>`) is intentionally unchanged — this is a policy constraint on implementations, not the trait API. | P3+ (GA) | security-engineer, rust-staff-engineer |
| R-SPAKE2-AUDIT | **`spake2` crate is UNAUDITED (P3-3).** The RustCrypto `spake2 = "=0.4.0"` crate used for unattended PAKE pairing carries the disclaimer "USE AT YOUR OWN RISK" and has not been independently audited. Mitigations in place: (a) all `spake2` types are wrapped behind `PakeExchange`; no raw `spake2` API is public; (b) an explicit HKDF-SHA-256 key-confirmation MAC is layered over SPAKE2 output, binding confirmation to `h` + both device identities so a `spake2` internal MAC bug doesn't directly yield a pinning bypass; (c) two fuzz targets (`pake_msg_parse`, `pairing_code_parse`) guard the parser surface; (d) `cargo audit` is clean; (e) `curve25519-dalek` dependency is unified at v4.1.3 (same as ed25519-dalek / x25519-dalek). **A full third-party security audit of `spake2` (and our `PakeExchange` wrapper) is a mandatory blocker before GA for any deployment that uses unattended pairing.** Track alongside the `snow` audit (R-SNOW-AUDIT). | GA | security-engineer |
| R-SIG-TLS | **Signaling server uses plain WebSocket; WSS TLS is not terminated in-process (P4-1).** Production deployments MUST put a TLS-terminating reverse proxy (nginx, Caddy, AWS ALB) in front of `SignalingServer`. The `insecure-lan` feature gates `AcceptAll` (any peer admitted) and is fenced out of release builds by a `compile_error!` guard; production code supplies `IdentityProofAuthenticator` (peer auth is now wired — see R-SIG-AUTH / ADR-0016). The zero-knowledge invariant (payload never parsed by the server) is structural and not dependent on TLS. R-SIG-TLS remains: signaling transport confidentiality still relies on a TLS-terminating reverse proxy. | P4 / GA | security-engineer, network-engineer |
| R-SIG-AUTH | **SATISFIED in code (ADR-0016).** Signaling peer authentication is implemented: the production `IdentityProofAuthenticator` requires every connecting peer to present an Ed25519 **possession-of-identity-key proof** (`sh_crypto::peer_auth::IdentityProof`) over a **fresh server-issued 32-byte challenge** (new `MessageKind::Challenge`, kind = 7), carried in the opaque `Hello` payload. The server verifies the echoed challenge (anti-replay, constant-time), key validity (rejects small-order), `Fingerprint::from(pubkey) == from_fp` (constant-time), and the signature (`verify_strict`) over a canonical, domain-separated TBS binding `session_id || pubkey || challenge`. A peer can no longer register a fingerprint it does not control; replay (including by a malicious relay) is defeated by the per-connection challenge — chosen over a self-signed time-boxed token precisely because the relay is untrusted (see ADR-0016). The challenge-nonce path requires no server-side challenge store (held per-connection). The `PeerAuthenticator` seam now takes `AuthContext` → `Result<(), AuthError>` (sanitized, uniform wire reason = no enumeration oracle) and remains a trait so allow-list / issuer policy can layer on. `AcceptAll` + the `insecure-lan` `compile_error!` release fence are preserved (tests only). Fuzz target `peer_auth_decode` covers the hostile proof decoder; `verify_strict` + `subtle` constant-time compares throughout. **Boundary:** server-side auth proves *ownership at the relay*, NOT end-to-end peer *trust* (that remains Noise/BindCert/TOFU, P3). **Residual:** transport confidentiality of signaling is still R-SIG-TLS (plain-WS + reverse-proxy TLS); deployment-level rate-limiting of auth failures and an allow-list policy impl are future, non-blocking. Minor test/UX follow-ups (non-blocking, from the code-review gate): (a) an integration test proving the fresh-challenge-per-reconnect path under `connect_authenticated` (the code re-signs each attempt; behaviour correct but unasserted end-to-end); (b) surface a server `Error` during the Hello-ack as a distinct `SignalingError::ServerError`/`AuthenticationFailed` instead of the opaque `UnexpectedMessageType` (pre-existing client gap, so callers can distinguish auth-reject from protocol error). | DONE (ADR-0016) | security-engineer |
| R-ICE-LIVE-NAT | **Live STUN/TURN server communication and real NAT traversal are deferred (P4-3 partial).** `sh-ice` (P4-3) delivers the complete TURN client codec (Allocate/Refresh/CreatePermission/ChannelBind/Send/Data), long-term credential authentication, ChannelData framing, and a SimTurnServer — all verified hermetically. What remains deferred (R-COTURN-DEPLOY): (a) live coturn deployment and the coturn REST credential endpoint integration; (b) `IceAgent::gather()` relay candidate gathering wired to a live TURN server; (c) TURNS-on-443 TLS configuration; (d) live NAT traversal on real internet paths. **Do not use `sh-ice` for production ICE until R-COTURN-DEPLOY is resolved; the current agent produces host candidates only, and the TURN client has never spoken to a real server.** | R-COTURN-DEPLOY | network-engineer, devops-engineer |
| R-COTURN-DEPLOY | **coturn server deployment and live TURN wiring are deferred from P4-3.** Deliverables: (a) coturn server deploy (Kubernetes manifest + Helm chart in `aws-eng-compute`); (b) TURNS-on-443 with TLS termination; (c) `IceAgent::gather()` extended to issue live Allocate requests and add `CandidateKind::Relay` candidates; (d) coturn REST HMAC credential endpoint wired to `TurnCredentials::generate`; (e) live connectivity test from two peers behind real symmetric NAT boxes. Blocking for GA; not blocking for P4-4 (WebRTC transport) or P4-5 (DTLS fingerprint binding). | GA | network-engineer, devops-engineer |
| R-ICE-FUZZ | **Schedule a nightly fuzz job for `stun_decode` and `turn_decode` (sh-ice/fuzz).** Both libFuzzer targets exercise decode paths against hostile bytes. The fuzz crate compiles (verified) but no nightly CI fuzz job exists yet (tracked in X-2). Fuzz continuously in an automated job; any panic that emerges is a security regression. | X-2 / GA | qa-engineer |
| R-WEBRTC-LIVE | **Live WebRTC / DTLS / SRTP over real network is deferred from P4-4.** `PinnedWebRtcTransport` + `WebRtcChannel` implement the `Transport`/`Channel` traits and the DataChannel loopback test (ICE+DTLS+SCTP+DataChannel) passes over a synthetic loopback. **Item (a) IMPLEMENTED (`feat/webrtc-drive-loop`, ADR-0018):** tokio drive task binding a real UDP socket and running the `drive()`/`handle_receive()` loop (using `next_drive_at()` for scheduling). Deliverables: `AsyncUdpSocket` trait (injectable async socket seam), `TokioUdpSocket` (production `tokio::net::UdpSocket` wrapper), `SimUdpSocket`/`SimNetwork` (deterministic in-memory channels for tests), `spawn_webrtc_driver` + `DriverHandle` (tokio task with oneshot-channel shutdown). Determinism via base-instant offset: `std::time::Instant` for str0m derived from `tokio::time::Instant` offset — under `tokio::time::pause()` gives bit-identical clock sequences. Tests: `sim_socket_send_recv`, `sim_socket_unknown_dest`, `sim_socket_local_addr`, `driver_shutdown_clean`, `webrtc_driver_sim_handshake_deterministic` (full ICE+DTLS+SCTP+DataChannel round-trip, paused time, 5 tests pass); `webrtc_driver_real_udp_loopback` (`#[ignore]`, real UDP). No new production deps; `tokio/test-util` added as dev-dep. `std::sync::Mutex` is never held across `.await`. What remains deferred: (b) wiring `PinnedWebRtcTransport` into `sh-core` (requires the P4-6 negotiation layer — now landed); (c) live `PinnedWebRtcTransport`↔browser RTCPeerConnection interop (P5 — needs SDP offer/answer via `sh-signaling`); (d) TWCC-based per-packet arrival-time feedback for GCC (currently `GccController` consumes `TransportStats.queue_delay` as a trendline proxy; real TWCC extraction from str0m stats arrives with P4-5/P4-6); (e) live coturn relay path through `PinnedWebRtcTransport` (blocked on R-COTURN-DEPLOY). **P5 security gate CLOSED (pre-P5 follow-up, `feat/pinned-webrtc-transport`):** the P4-6 `TransportFactory` API footgun is now closed by the `PinnedWebRtcTransport` builder (ADR-0017): `WebRtcTransport::new` is `pub(crate)` and `set_remote_dtls_fingerprint` is removed from the public API; the only path to a `Transport`-implementing WebRTC type is `WebRtcTransportBuilder::new(...).pin_remote_dtls(fp)`, enforced by the type system. The P5 live-socket wiring task inherits this requirement — it MUST use the builder. **Doc-honesty invariants:** P4-4 does NOT bind DTLS to device identity (that is P4-5 BindCert); does NOT feed live str0m stats into GCC (P5 drive loop). Per-channel recv queues are capped at 512 frames to bound memory from flooding peers. `PinnedWebRtcTransport` is fully functional at the sans-IO level and the drive-loop contract is documented. | P5 | network-engineer, security-engineer |
| R-DTLS-EXPORTER-BIND | **WebRTC Noise-prologue ↔ DTLS-exporter channel binding is deferred from P4-5.** ADR-0007 §1.4 envisaged binding the Noise prologue's `session_context` to the WebRTC DTLS exporter (mirroring native QUIC's TLS-exporter binding), so the Noise run is cryptographically pinned to *this* DTLS connection. It is deferred because it conflicts with the P4-5 pin-before-handshake ordering: the DTLS exporter does not exist until *after* the DTLS handshake, but the identity-signed `BindCert` (carrying `DTLS_FPR_COMMIT`) must be pinned *before* DTLS starts. **This does NOT weaken the P4-5 MITM defense:** the fingerprint pin already binds the peer's DTLS certificate to the trusted Ed25519 identity (str0m fail-closes a mismatch), and the `ALG=NONE` downgrade is rejected for WebRTC peers. The residual gap is only the *additional* anti-lift property (preventing replay of the Noise messages onto a different DTLS connection sharing the same identity), which the cert pin makes low-value. Resolution options to evaluate: (a) a post-DTLS key-confirmation exchange over the established Noise+DTLS channel that mixes in the DTLS exporter; (b) a two-phase prologue. See ADR-0014 §5. **P4-6 structural gate CLOSED (pre-P5 follow-up, `feat/pinned-webrtc-transport`, ADR-0017):** the `PinnedWebRtcTransport` builder makes it structurally impossible to construct a WebRTC transport without applying the DTLS pin — `WebRtcTransport` is `pub(crate)` and `set_remote_dtls_fingerprint` is removed from the public API. The prerequisite for landing R-DTLS-EXPORTER-BIND (the structural enforcement that prevents a production factory from silently omitting the pin) is now satisfied. **P4-6 invariant (carried from P4-5):** the `sh-core` session orchestrator MUST call `require_webrtc_dtls_pin()` (→ typed `DtlsBindingMissing` abort) for EVERY DTLS-bearing (WebRTC) session before its DTLS handshake, so transport-capability negotiation cannot route a session onto unpinned DTLS. str0m fails closed regardless (it refuses to complete DTLS without a pinned remote fingerprint), but the orchestrator-level requirement makes the anti-downgrade gate explicit and non-bypassable at the negotiation seam. | P5 / GA | security-engineer, network-engineer |
| R-STR0M-AUDIT | **`str0m` crate is UNAUDITED (P4-4).** `str0m = "=0.20.0"` is the sans-IO WebRTC engine that parses DTLS, STUN, RTP, and SCTP bytes from hostile network input. It has not been independently audited. Mitigations in place: (a) all `str0m` types are wrapped behind `WebRtcTransport`/`WebRtcChannel`; no raw str0m API surface is public; (b) per-channel receive queues are capped at 512 frames (`MAX_RECV_QUEUE_DEPTH`) to prevent memory exhaustion from a flooding peer; (c) `fingerprint_verification` is NOT disabled in production — the remote DTLS certificate is verified by default; (d) `cargo audit` is clean; (e) exact version pin ensures a deterministic dependency tree. **Dependency reality:** `str0m`'s transitive dep `dimpl` pulls `aws-lc-rs` AND a second `rcgen 0.14` into the tree alongside `ring`/`rustls`; see ADR-0013 for the justification. **A full third-party security audit of `str0m` (DTLS, STUN, ICE, SCTP parsers) AND a `webrtc_framing` fuzz target are mandatory blockers before GA.** Any `str0m` version upgrade requires a security review before merge. Tracked alongside `R-SNOW-AUDIT` and `R-SPAKE2-AUDIT`. | GA | security-engineer |

---

*Update this document in the PR that changes status. It is the canonical answer to "where are we?"*
